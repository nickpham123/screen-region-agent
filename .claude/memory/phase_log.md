# Phase Log

Status of each phase from `implementation_plan.md`. Update the status column as work happens; add a one-line note on what moved.

| Phase | Description | Status | Note |
|---|---|---|---|
| 0 | Scope decisions | ✅ Done | OS target still open (see decisions.md) |
| 1 | Desktop prototype (Electron + Mistral-backed, multi-turn Chat Panel) | 🟡 In progress | Design complete (system_design_plan.md). Steps 1-4 of 8 built, verified, and closed (scaffold, hotkey listener, selection overlay, `desktopCapturer` + crop — the last verified on both displays 2026-08-22, see the log entries below); **Step 5 (Chat Panel) core loop done** — window lifecycle, temp-file lifecycle invariant, hold-to-talk key mechanics, text field + message history, and voice recording/transcription (`nodejs-whisper`, after `whisper-node-addon` was confirmed broken) all built and hands-on verified 2026-08-22/23; **Step 6 (Vision Model Client) core call done** — `askAboutRegion(imagePath, conversationHistory)` implemented in `visionClient.js` and verified 2026-08-23 against the real Mistral API (hybrid full-screenshot context deferred as a tracked follow-up); **Step 7 (Response Handler) fully built and verified 2026-08-24** — real multi-turn loop, cancel-on-close, and all `.code`-based error/retry paths all genuinely exercised, not just reasoned through (see the log entry below for the full arc, including a regression scare that was investigated and left unreproduced); **Step 8 (Local Logger) implemented and fully verified 2026-08-24** — `logConversation()` writes JSONL per system_design_plan.md §5, verified via `diagnostics/verify_local_logger.js`, a real end-to-end Chat Panel session (happy path), and a real mid-request panel close confirming `trimToCompleteExchanges()`'s dangling-turn trim on disk, not just synthetically (see the log entries below). **All 8 designed steps of Phase 1 are now built and hands-on verified, unconditionally.** Step 4 is not yet reviewed/merged to `main` the way Steps 1-3 were. Correction: an earlier note here mistakenly cited the superseded Python prototype (capture.py etc.) as "loop written" — those files don't count toward the current Electron plan, see project_state.md |
| 2 | Instrumentation + Main App Window | 🟡 In progress | Redesigned this session (2026-08-24): storage/schema is **already built** — that was Phase 1 Step 8's Local Logger, not Phase 2 work, correcting this row's stale "schema designed, no code." Scope now also includes a persistent Main App Window (Captures/Settings/Help) — see implementation_plan.md's 2.0–2.7 step list and decisions.md. Steps 2.0 (codebase reorg), 2.1 (feedback capture, including the `feedback_note` schema addition), and 2.2 (Main App Window shell, including a real Dock-visibility bug found and fixed) are all done and hands-on verified; **2.3 (Settings page + shared theme layer) implemented 2026-08-25, hands-on verification still open** (smoke-tested only — see the log entry below and todo.md's per-control checklist); 2.4–2.7 (Captures page, Help page, Chat Panel enhancements, app_hint) not started |
| 3 | Dataset curation & synthetic bootstrap | ⬜ Not started | Blocked on Phase 1/2 producing real data |
| 4 | Fine-tuning on Swan | ⬜ Not started | Blocked on Phase 3 |
| 5 | Evaluation | ⬜ Not started | Blocked on Phase 4 |
| 6 | Serving the fine-tuned model | ⬜ Not started | Blocked on Phase 5 |
| 7 | Productionization (real users) | ⬜ Not started | Deliberately deferred — don't start early |

**Legend**: ✅ Done · 🟡 In progress · ⬜ Not started · 🔴 Blocked

## Log

Add a dated entry whenever a phase's status changes.

- 2026-08-25 (latest — Phase 2.3 Settings page + shared theme layer implemented, plan revised twice on review, hands-on verification still open): **Plan proposed before building, per CLAUDE.md's mentor-mode rule, and revised twice from the user's actual feedback rather than proceeding on the first draft.** Round 1 (4 flagged questions — hotkey save timing, icon hosting, dictation UI shape, hotkey test mechanism) came back with one item auto-approved-by-default in the tool's answer field contradicting the user's own prose explanation for it (hotkey save) — followed the prose, not the field, per the harness's explicit instruction to do so. Round 2 pushback: the recommended English/Multilingual auto-detect toggle was rejected in favor of an explicit pinned-language picker (auto-detect is less reliable on short hold-to-talk clips, and pinning doesn't cost more to verify than the toggle would have); the hotkey test design was approved with two additions — a static Option/Alt pre-check before spending a live gesture-test cycle on a known failure, and resolving whether the production hotkey stays live during a test (resolved: no, reusing the single-flight gesture state for the bounded test duration, distinguished explicitly from the earlier concurrent-session-guard precedent it might otherwise look like it was repeating) — plus a UI-only "hotkey paused while testing" indicator. **Built**: `src/shared/theme.css` (tokens + `prefers-color-scheme`, `nativeTheme.themeSource` propagates automatically — no IPC/data-theme plumbing needed), `src/shared/settings.js` (`userData/settings.json` read/write + the `DICTATION_LANGUAGES` lookup), the icon webfont vendored as two static files after installing the actual npm package revealed a 121MB/203-package footprint for ~673KB of real runtime need (own decisions.md row, not folded into the theme work, per explicit instruction), `activateGestureOverlay()` extracted from `registerHotkey()` so the hotkey test reuses the exact production gesture path, all six Settings rows in `mainWindow.html`. A real API-shape check caught `nodejs-whisper`'s `language` option living under `whisperOptions.language`, not top-level as the name alone might suggest — passing it at the top level would have silently done nothing. Full reasoning for every piece in decisions.md (5 new rows). **Verification status, stated plainly**: smoke-tested only (`npm start`, no crash, hotkey still registers, no lingering process) — no Settings control has been clicked, and the hotkey record→test→Save flow specifically needs a real hold-drag-release cycle with actual hands, the same bar this project has applied to every accelerator choice so far. See todo.md for the per-control checklist this leaves open.
- 2026-08-25 (Phase 2.2 Main App Window shell built, a real bug found and fixed, hands-on verified): **Main App Window shell (2.2) built per the plan proposed and confirmed before implementation**: lazy creation (no window on launch — hotkey-first), `showOrCreateMainWindow()` idempotent entry point, hide-not-destroy on close with an `isQuitting` guard so `Cmd+Q` still really quits, dock-icon-only entry point for now (a custom app menu explicitly deferred — risked silently breaking `Cmd+Q`/Edit/copy-paste if the default template wasn't reconstructed correctly, same failure shape as this project's hotkey history), `src/shared/settings.js` explicitly deferred to 2.3 (no caller yet). **First real verification attempt surfaced a genuine bug, not a testing artifact**: no Dock icon ever appeared, from any launch method (this session's background launch, sandbox explicitly disabled, and the user's own interactive Terminal) — root-caused via a throwaway diagnostic script + `osascript`'s `System Events ... background only of process "Electron"` query (confirmed `true` before, `false` after calling `app.dock.show()`), not assumed from Electron's docs. Fixed with one line in `app.whenReady()`. **A second, unreproduced anomaly followed**: the Dock icon disappeared again mid-session on one real run after the fix, independently confirmed via the same `osascript` query at the time. Investigated properly rather than dismissed: temporary `[dock-diag]` logging added at every real lifecycle checkpoint (window creation/reuse, close-intercept before/after `hide()`, both `app.focus({steal:true})` call sites), then a fully-instrumented rerun with equivalent-or-heavier interaction showed zero anomaly across 4 dock activations, a close-intercept, and a real `Cmd+Q` — left as a genuinely unreproduced single observation with a concrete revisit trigger, not silently dropped. Diagnostic logging removed once its purpose was served. **All 5 verification checks now have real evidence**: dock click opens the window (log), sidebar switching shows each placeholder view (confirmed directly by the user — inherently un-loggable, pure client-side JS), close hides without quitting (log), a second dock click reshows instantly without recreating the window (log), and `Cmd+Q` genuinely quits (log **and** independently via `ps` showing the process gone). This closes Phase 2.2.
- 2026-08-25 (Phase 2.1 feedback capture built and hands-on verified, plus two decisions resolved): **Two open items from the prior session resolved, then Phase 2.1 built.** (1) Main Window close-vs-quit: resolved as normal macOS dock-app convention — closing the window doesn't quit (already correct today via `window-all-closed`'s existing darwin no-op) and `Cmd+Q` already quits fully (Electron's default app menu, never overridden by `Menu.setApplicationMenu()`); one real gap flagged for Phase 2.2, not built now — an `app.on('activate', ...)` handler to reshow the window from the dock icon. (2) A new fine-tuning goal — response quality (helpfulness, concision, tone), not just image-reading accuracy — recorded with its tension against Phase 5's existing "not conversational flair" eval stance named explicitly, not silently blended in; resolved on the lighter-weight data-collection approach (broadened Phase 3 hand-review + an optional 👎-time note) over two heavier alternatives (per-turn feedback, a preference-pair UI), both recorded as considered-and-deferred. **Phase 2.1 built**: 👍/👎 buttons in the Chat Panel title bar, plus a schema addition that came with it — `feedback_note` (optional free-text, 👎-only, never required). Hands-on verified via 4 real sessions covering all 3 required scenarios (👍/no-note ×2, 👎/with-note, 👎/no-note), confirmed in `conversations.jsonl` and against the permanent crop files on disk. `data_pipeline.md` and the `dataset-pipeline` skill's hand-review workflow both broadened to cover quality/concision/tone (not just correctness), including on 👍'd conversations, per the response-quality decision.
- 2026-08-24 (Phase 2 redesigned, codebase reorg (Step 2.0) landed and hands-on verified): **Phase 2 status corrected and rescoped.** This row previously said "Schema designed, no code" — stale twice over: the schema/storage was actually already built as Phase 1 Step 8's Local Logger, and Phase 2 itself was redesigned this session to add a persistent Main App Window (Captures/Settings/Help, single `BrowserWindow`, sidebar client-side view switching) alongside the original instrumentation scope (feedback capture, `app_hint` auto-tagging) — full step list now in implementation_plan.md (2.0–2.7), decisions in decisions.md. **Step 2.0 (reorg) executed and verified**: `main.js` slimmed (880 lines, real god-file risk, not hypothetical) by moving `overlay.{html,js}`/`preload.js` → `src/overlay/`, `chatPanel.{html,js}`/`chatPanelPreload.js` → `src/chatPanel/`, `capture.js`/`visionClient.js`/`responseHandler.js`/`localLogger.js` → `src/shared/`; two `diagnostics/` files' relative requires updated to match; the dead Step 1 placeholder (`index.html`, `createWindow()`/`win`) deleted in the same pass rather than carried forward, since its own todo.md cleanup condition ("once Electron reaches equivalent functionality") is satisfied by the Main Window landing in this same effort. Committed as `8d787cf` on `phase-2`, independently reviewed against the actual diff. **Smoke-tested both halves, not just the mechanical move**: `npm start` launches with no require/loadFile errors and the hotkey registers (first half); a real hold-drag-release gesture then ran end to end (second half) — log showed the full activation→capture→vision-call→close sequence, the permanent crop/full files were confirmed to exist at the exact logged paths with real matching content (the crop image was opened directly and genuinely showed the circled on-screen text), the temp directory was empty afterward confirming a genuine move not a copy, and the new `conversations.jsonl` line matched system_design_plan.md §5's schema field-for-field. One honest note from that gesture: Mistral's transcription had two minor misreadings against the real image content — a model-accuracy quirk unrelated to the reorg or the pipeline's correctness.
- 2026-08-24 (trim-on-cancel verified with a real mid-request close — Phase 1 now unconditionally verified across all 8 steps): **The one remaining Step 8 gap, closed.** A real session: first question submitted and answered normally (2 turns), a follow-up submitted (`Vision call starting (3 turn(s) in history)`), panel closed immediately, before the reply arrived. The log itself showed the trim happening before the cancellation even settled — `Chat Panel closed with 2 turn(s) — moved to permanent storage and logged` fired first, `Vision call cancelled (panel closed mid-request)` after — confirming `endChatSession()` computes `trimToCompleteExchanges()` synchronously at close time, independent of when the aborted promise's `.catch()` gets scheduled. Verified on disk, not just the log: the new `conversations.jsonl` record contains exactly the 2 real turns from the answered exchange; the dangling, unanswered follow-up question is genuinely absent from the array, not merely uncounted — the actual failure mode §7 warns against (logging a partial/broken conversation) is confirmed not to occur. Permanent crop/full files present with correct content, temp folder empty afterward. This was the one Step 8 code path that hadn't had the same real-usage verification treatment as the temp-file move, JSONL write, and schema shape — all of Step 8, and all 8 of Phase 1's designed steps, are now hands-on verified with no remaining conditional exception.
- 2026-08-24 (Step 8 real end-to-end verification — closes Phase 1's designed 8-step plan for the happy path): **Real Chat Panel session run end to end to confirm Step 8, not just the diagnostic.** Real `Control+1` gesture (one below-minimum-size attempt correctly ignored first, then a real finalized selection), 3 real submit→reply exchanges against the actual Mistral API (6 turns total), real panel close. Verified directly on disk rather than trusting the log alone: both permanent PNGs exist at the paths the log reported, with real correct content (1094×810 crop, 2992×1934 full screenshot) and the temp folder was empty afterward, confirming a genuine move; `conversations.jsonl` (the real app's `userData` path, not the diagnostic's) contains exactly one line, valid JSON, matching system_design_plan.md §5's schema field-for-field — `turns` nested with all 6 real exchanges in order, a real uuid `id`, `source: "real_usage"`, `category`/`feedback` correctly `null` (no capture UI for either yet), `started_at`/`ended_at` consistent with the session's real timeline in the app's own log. Along the way: corrected a wrong claim made in the same session about this project's commit convention — checked directly against git history (Steps 3-7, `bb3ac48`/`34dbad5`/`0fecc44`/`1ace68e`, were each self-reviewed and pushed to a feature branch, then merged into `main` via a real GitHub PR (#1) — not committed straight to `main`) rather than defaulting to a generic "branch first" instinct that doesn't match this project's actual practice. `main` and the branch never actually diverged, so local `main` stayed in sync throughout the project's history — that conclusion holds — but the mechanism was a PR merge, not a direct commit. Also confirmed via the `run` skill that this app's core gesture (global `Control+1` hotkey) genuinely cannot be driven by synthetic/Playwright-injected input — `globalShortcut` is dispatched by macOS itself, outside any window's event loop, which is also *why* this project's entire hold/release debugging history in decisions.md exists — so this verification, like every "real gesture" verification before it in this project, was performed by the user's own hands while Claude watched the log and then checked the resulting files directly. One remaining honest gap: the dangling-trailing-turn trim path (a session cancelled mid-request) wasn't exercised by this real session, since every exchange completed normally — that behavior remains diagnostic-verified only, not yet hands-on-confirmed with a real cancel.
- 2026-08-24 (Step 8 implemented and verified via diagnostic): **`localLogger.js`'s `logConversation()` built, wired into `main.js`'s `endChatSession()`, and verified.** Before writing code, walked the design through per CLAUDE.md's mentor-mode rule and got explicit sign-off on each piece: (1) JSONL over SQLite — initially recommended on native-addon-packaging-risk grounds (this project had already been burned twice, `whisper-node-addon`/`nodejs-whisper`), but the user asked `node:sqlite` be checked empirically before locking that in, since it removes that exact risk (built into the Node/Electron binary, not a separate compiled artifact). Checked directly against Electron 43.4.1's real bundled runtime via `ELECTRON_RUN_AS_NODE`: confirmed Node 24.18.1, `DatabaseSync` loads and runs real SQL correctly, and — a genuine surprise — the `ExperimentalWarning` that fires reliably on system Node v23.9.0 does not fire in this exact bundled version, despite `--experimental-sqlite` still being a registered flag on both. JSONL was still chosen after that finding, on updated grounds (querying isn't needed by anything in current scope, `turns` would just become a JSON TEXT column either way, and `data_pipeline.md`'s curation workflow is hand-editing a JSONL line in a text editor) — `node:sqlite` recorded as evaluated-and-viable, not ruled out for risk, with a concrete re-verify trigger in todo.md. (2) A real gap found by tracing, not assumed away: the existing `turns.length === 0` keep/discard check would have logged a broken conversation for a session cancelled mid-request (§7's already-verified cancel-on-close leaves a dangling unanswered user turn, length ≥ 1). Fixed with `trimToCompleteExchanges()` — drops a trailing unanswered user turn before the keep/discard decision and before logging, safe because `turns` is strictly append-only. (3) Caught and fixed a real discrepancy in this project's own memory: `project_state.md` and this file both previously claimed `data_pipeline.md`/`skills/dataset-pipeline/SKILL.md` were "already updated" for the multi-turn schema — false, both still showed the old flat per-question shape until fixed in this same pass. **Verified**: `diagnostics/verify_local_logger.js` (run via `npx electron`, since `logConversation()` needs `app.getPath('userData')`) confirmed `trimToCompleteExchanges()`'s three cases (well-formed untouched, dangling-trailing-turn trimmed to the real pair, single-unanswered-question trims to zero) and `logConversation()` appending exactly one well-formed JSONL line matching every §5 field. Also smoke-tested: `npm start` launches cleanly with the new `require`, hotkey registers, no crash. **Not yet done**: a real end-to-end Chat Panel session (real gesture → real turns → real close) exercising this code path live, as opposed to the diagnostic driving it directly.
- 2026-08-24 (Step 7 fully built and verified, including a real regression investigation): **Response Handler implemented (`responseHandler.js` + wiring), then genuinely verified end to end — not just on the happy path.** First verification pass confirmed the core loop twice with real evidence (accurate, specific multi-turn replies; loading indicator visually confirmed) but got sidetracked into a regression chase before cancel-on-close or error routing were ever tested — caught on review before "Step 7 complete" was accepted, and both were then actually exercised: a real cancel-mid-request (panel closed 1.5s into a live call, confirmed via log chain that it aborted cleanly with no phantom turn logged) and all 5 reachable `.code` paths (network via a closed port, rate_limit via a real HTTP 429 from a local mock server, two `malformed` shapes, api_error via both a mock 500 and — more realistically — the real Mistral endpoint with a deliberately invalid key, real HTTP 401) each hitting the real classification code and producing the exact §7 message with no phantom turn; retry confirmed to actually re-send the unanswered turn and succeed once fixed. Along the way, a real-looking regression surfaced ("Incident B"): 3 consecutive gesture failures (zero keyboard events for a genuine multi-second hold) immediately after a chat session that exercised the new loading-state disable/focus cycle. Investigated with temporary diagnostics, which contained a self-inflicted bug (`app.isFocused()` isn't a real Electron API — crashed the main process) fixed along the way. Whether the crash fix was itself "the fix" was explicitly reasoned through (chronologically impossible, mechanically read-only) rather than assumed, and then an actual no-diagnostics control test was run on request rather than accepting the reasoning alone: reverted the diagnostics entirely, reproduced the exact failing sequence 3 times in a row, all clean. Left as an unreproduced anomaly consistent with this project's already-documented intermittent hold/release flakiness, not a confirmed Step 7 regression — with a concrete revisit trigger if it recurs. Full evidence trail in decisions.md.
- 2026-08-23 (Step 6 core call done): **`askAboutRegion(imagePath, conversationHistory)` ported to JS and verified against the real Mistral API, closing Step 6's core scope.** Before writing code, walked through the design per CLAUDE.md's mentor-mode rule (contract mapping, API key location, failure surfacing per §7) and raised an explicit scope question: build `todo.md`'s full Step 6 notes (thumbnail hybrid context + tool-calling escalation + prompt caching) now, or the core call only. User approved scoping down to the core call, with the rest tracked as a deliberate follow-up before Step 7 — keeps the first real external-API integration in this codebase isolated from three other new sources of complexity. Implementation (`visionClient.js`): every `conversationHistory` turn is a plain `{role, content: string}`, with the image wrapped into Mistral's content-parts shape on turn 0 fresh on every call (private to this module, not caller-visible) — sufficient because the API is stateless and the full array is rebuilt every call anyway. Failures throw an `Error` with a `.code` (`network`/`rate_limit`/`api_error`/`malformed`), not formatted text — routing that to §7's user-facing messages is Step 7's Response Handler job, matching the §3.5/§3.6 split. A 30s request timeout was added and justified in-code (not a bare constant). API key loads from a local `.env` via `dotenv`, per the user's standing instruction that keys are set directly into a file by them, never relayed through chat or a shell command either party runs; `.env` confirmed gitignored and never committed. Verified via `diagnostics/verify_visionclient.js` against a real crop already on disk and the real API: single-turn call correctly described actual image content (a code screenshot, described accurately down to the specific logic shown); a follow-up call against the growing history returned a coherent, consistent condensed answer rather than a fresh unrelated one, confirming history round-trips; a missing-key case threw the expected clear error. Not yet wired into the live Chat Panel — that's Step 7.
- 2026-08-23 (latest, closing Step 5's core loop): **Voice recording implemented, tested, and fixed twice based on real usage, closing Step 5's core loop.** Design walked through and approved before writing: raw PCM capture in the renderer (`ScriptProcessorNode`), a hand-written 16kHz WAV (deliberately avoiding `ffmpeg`, which isn't installed and would've stacked a second Phase 7 packaging risk on `whisper-cli`'s own), a duration guard keyed on actual captured audio length rather than hold time (per explicit user refinement — a short utterance shouldn't be silently dropped), and visible feedback for every non-happy path. First hands-on round: 3 real dictations transcribed accurately, 16kHz honored exactly by real mic hardware (no resampling needed) — but surfaced two real gaps: the first-ever hold hit the watchdog timeout instead of a real keyup — a single, unreproduced anomaly; one hypothesis is the already-tracked missing-focus-listener gap, but that's unconfirmed (never instrumented, hasn't recurred in 8 subsequent attempts), so it's recorded as an open anomaly, not a settled observation — and multiple dictations were replacing the field instead of accumulating. Fixed the replace-vs-append issue; a second round then surfaced a further real gap the fix exposed — a single-line input only showed the tail of a growing message. Fixed by switching to an auto-growing textarea with the standard Shift+Enter-for-newline convention. Re-verified after both fixes: 2 more real multi-segment dictations, confirmed correct by the user. Step 5's window lifecycle, temp-file invariant, hold-to-talk mechanics, text/history, and voice are now all built and hands-on verified — what remains (Step 6/7's real model call, Step 8's real logger) isn't Step 5's job anymore.
- 2026-08-23: **Text field + message history implemented and hands-on verified; voice backend switched from `whisper-node-addon` to `nodejs-whisper` after the former was confirmed broken.** Text/history: `#input` submits turns on Enter, rendered into `#history`, sent to `main.js`'s already-scoped `chat-turn-added` listener. 3 real turns submitted in one session, panel closed — this was **the first time `endChatSession()`'s "keep" (move-to-permanent) branch ever fired end-to-end**, closing a gap tracked since earlier the same day. Verified on disk: files landed in `userData/captures/` at byte-identical sizes to temp, temp emptied (genuine move), crop image opened cleanly with real content. Separately: `whisper-node-addon`'s published `darwin-arm64` binary was confirmed broken via direct testing (`dlopen` fails on its own `libwhisper.1.dylib` — a CI-machine-specific rpath baked into the build, not fixable via standard `DYLD_LIBRARY_PATH` workarounds) — this went well beyond the package's own "early experimental" README label the user had initially agreed to proceed past, so it was surfaced with full evidence and options rather than patched around unilaterally. Evaluated `nodejs-whisper` (the broken package's own README-recommended alternative) as a real replacement, not just docs-read: installed `cmake` (via Homebrew), let it compile whisper.cpp from source, and ran a real transcription. Both properties that justified the original backend choice were explicitly re-verified rather than assumed to carry over — on-device (only the one-time model download touched the network) and genuine Metal acceleration (the runtime's own logs confirmed `MTL0 (Apple M1 Pro)` actually in use, not just compiled in). `whisper-node-addon` uninstalled, clearing an unrelated `npm audit` critical finding to zero in the process. Voice recording/transcription itself is still not wired into the app — this closes the backend question, not the feature.
- 2026-08-23 (later): **Hold-to-talk's accelerator conflict investigated and resolved before any voice-input code was written, then hands-on verified.** Raised on review: does `Control+1`, left registered under the just-approved concurrent-session guard, still claim `Digit1` even once the Chat Panel already has real OS focus? Verified empirically with a temporary diagnostic rather than trusting decisions.md's existing row on this (which turned out to be wrong) — 3 of 4 real test presses showed `Digit1` never reaching the renderer at all, for as long as the key was held (one held 4.2s), while the 4th showed a fully clean, immediate, normal sequence with the global accelerator not firing at all. The split is explained by an already-established finding, not a new one: macOS only matches the global accelerator when the modifier goes down before the trigger key, and the renderer's own timestamps showed the one clean case had `Digit1` arriving before `ControlLeft` — the reverse order. Since modifier-first is also the only order that registers the base hotkey at all, every real hold-to-talk attempt hits the claiming behavior, not occasionally. Fixed by branching the guard on `chatPanelWindow.isFocused()`: focused treats the press exactly like a region-selection gesture (unregister, bootstrap the renderer's held-state from the activation signal rather than a local keydown, run a dedicated liveness watchdog, end strictly on the real keyup), not-focused keeps the existing focus-the-panel behavior. Hands-on verified with 3 real hold cycles (~3.1s, ~8s, ~2.2s) — the 8s hold in particular only survives without a watchdog force-close if the liveness pings are genuinely working, not by chance. Real audio capture (whisper-node-addon) is still not built — this closes only the key-mechanics loop blocking it. decisions.md's superseded row now carries this session's evidence rather than being silently corrected.
- 2026-08-23: **Step 5's window-lifecycle/temp-file-lifecycle slice hands-on verified, closing that slice.** Before testing, corrected a decision made and reported after the fact rather than proposed first (see decisions.md): the original concurrent-session guard unregistered the hotkey for the whole Chat Panel session, which made a press while blocked a structurally silent no-op and stranded the hotkey with no watchdog if a session ever failed to close. Revised, with the user's sign-off, to keep the accelerator registered throughout and guard inside the callback instead — a press while a session is open now focuses the existing panel (visible, logged) rather than doing nothing, and the unregistered window stays scoped to gesture+capture only, unchanged from before Step 5. User then ran 4 real gestures across both the built-in and an external Dell monitor: panel opened and took real keyboard focus every time; re-pressing the hotkey while a panel was open (3 presses in one session) correctly hit the new guard every time instead of starting a second gesture; every close correctly discarded its temp files, confirmed by checking the temp folder directly (empty after each session) rather than trusting the log alone. Step 8's real `logConversation()` write and the actual chat UI (text/voice/history) remain unbuilt — next up.
- 2026-08-22: **Step 5 started — Chat Panel window lifecycle + temp-file lifecycle invariant implemented** (`main.js`, `chatPanel.html` placeholder, `capture.js` subfolder change). Before writing code, presented the two decisions.md/todo.md-flagged design forks to the user per the project's mentor-mode rule and got explicit sign-off on both: (1) the Chat Panel window is created fresh per session and closed (not hidden/reused) on close, chosen over the overlay's reuse pattern because it makes "history starts empty" free and gives one unambiguous close-time trigger for the temp-file decision, rather than requiring correct manual state reset each reopen; (2) the temp-file close-handler invariant (decisions.md: exactly one of move-then-log or delete-and-discard, never neither/both) is implemented now, with an EXDEV-safe move (`moveFile()`, rename with copy+unlink fallback) and `capture.js` fixed to write into the `temp/screen-region-captures/` subfolder the design already assumed. A third decision — the hotkey now stays unregistered for the whole Chat Panel session, not just during capture — was surfaced during implementation (not previously discussed in any doc) to prevent a second concurrent session/panel; recorded in decisions.md rather than decided silently. `moveFile()` verified standalone against real files (`diagnostics/verify_move_file.js`); app verified to launch cleanly (`npm start`, no errors). **Not yet built**: the actual text/voice/history UI, or Step 8's real logger (the "keep" branch stands in with a console log + `TODO(Step 8)` for now). **Not yet verified**: a real end-to-end gesture — needs the user's hands, same as Steps 3-4.
- 2026-08-22 (closed): **Step 4 crop correctness verified on both displays, closing this step's highest-risk item.** Method: opened the actual `crop.png`/`full.png` pairs from real gestures and cross-checked by converting each logged bbox to physical pixels (× the display's `scaleFactor`) and confirming that computed region of `full.png` matches `crop.png`'s content — not inferred from clean logs alone. Built-in display (2x): one of three gestures landed on live-chat disclaimer text and matched exactly, including the mid-word truncation. External QHD display (1x, previously zero coverage): one gesture landed on real terminal log text and matched **exactly across six lines, character-for-character** — the strongest result of the pass. No trail/badge/reset-flash contamination in any image on either display, confirming the deterministic-clear fix (see decisions.md) holds up in practice. Step 4 not yet formally reviewed/merged to `main` the way Steps 1-3 were — that's still open.
- 2026-08-22: **Step 4 implemented** (`capture.js`, plus additions to `preload.js`/`overlay.js`/`main.js`) — `captureRegion(bbox, displayId) -> {cropPath, fullPath}`. Preceded by a standalone verification script (`diagnostics/verify_capture_*.js`) run on real hardware, which confirmed the scaleFactor conversion formula, unshaped `getUserMedia` constraints, and `desktopCapturer` source↔display mapping all hold on both a 2x built-in and a 1x external display — and separately caught a real bug: `track.getSettings()` returns stale (previous-capture) dimensions on a renderer session's second capture, traced to the API layer itself (not test-harness object reuse) and confirmed to recur in the real app, which reuses one overlay renderer across every gesture. Fixed by reading `video.videoWidth/videoHeight` instead. Grounding the implementation in the actual `main.js`/`overlay.js` code (rather than just the docs) surfaced two more design points before writing it: display identity must be threaded from activation to finalize rather than re-derived (same "two independently-computed answers" bug shape as above), and the overlay must be hidden and given a settle delay before capturing, or its own drawn trail/badge would appear in the screenshot. Smoke-tested (`npm start` launches, hotkey registers, no crash) but **no real gesture has exercised the crop path yet on either display** — that's the next thing to close out.

- 2026-08-21 (multi-monitor): Overlay now follows the cursor's display (`5387246`), superseding the v1 primary-display-only scoping decision after a second monitor made that limitation fail *silently* — hotkey fired, logs looked healthy, overlay rendered unreachable. Required a second fix: a window moved to a secondary display renders and takes mouse events but never becomes the key window, because macOS runs a separate Space per display; `setVisibleOnAllWorkspaces(true)` resolves it. Failure was reproduced 5/5 before the fix; the Spaces *mechanism* is inferred, not measured (`isFocused` was never instrumented during the failing run). Verified on both displays including the cursor-vs-focus disambiguating case.
- 2026-08-21 (closed): **Step 3 fully closed, reviewed, and merged to `main`.** Two commits: `e9fa2ba` "Step 3 verified: Selection Overlay with reliable hold/release detection" (code + docs), and `4ee443b` "Guard against multi-instance contamination with a single-instance lock" (kept separate as a dev-environment guard, not a Step 3 behavior fix). A review pass before commit produced four corrections worth noting: the multi-instance dispatch *mechanism* was downgraded from stated fact to inference after checking Electron's docs (it is undocumented — see decisions.md); a figure was corrected from 34/35 to 32/33; the focus-instability retraction was restructured into two adjacent superseded/correction rows per this project's own rule after being wrongly rewritten in place; and `system_design_plan.md` §3.1/§3.2 were refreshed, having still described the pre-fix design. Next unstarted item: **Step 4 — `desktopCapturer` + crop**.
- 2026-08-21 (end of session): **Step 3 is now behaving correctly end to end.** Accelerator resolved to `Control+1` after hands-on testing of seven candidates — five registered successfully and still failed, each differently, which established "registration success proves nothing" as a hard rule. Two further findings from user observation rather than analysis: macOS requires the modifier down *before* the trigger key (this had been misattributed to Escape's key hardware), and input-method interception is a recurring failure class (`Alt+Space` and `Control+Space` broke identically). The activation-trail bug was closed after four wrong diagnoses; what cracked it was logging the anchor value rather than only the events — the bug lived in the variable assumed trustworthy. Focus instability was retracted as a testing artifact (stray Electron instances competing for the accelerator). Verified: deliberate holds 8-20s, fast taps closing at 2.5s, and press-while-still-moving all correct.
- 2026-08-21 (later): Root mechanism for the whole hold/release failure class finally identified — **`globalShortcut` consumes the trigger key's original keydown, leaving the overlay blind to the key until macOS auto-repeat starts (~445ms measured)**, and a key the window never saw produces no keyup either. This single fact explains the "intermittent quick-tap" bug (deterministic: release under ~445ms), the "hotkey sometimes does nothing" reports (the accelerator is unregistered for the duration of a hung gesture), and the lingering trail after a tap. Mitigated with a 2.5s initial-liveness deadline, sized above macOS's slowest configurable repeat delay so it can't truncate genuine holds. Also fixed: the phantom activation trail — caused by mixing the main process's activation point with the renderer's `clientX/clientY` (they disagree by a few px for the same stationary cursor), not by the suspected residual point, which was measured and disproven. `decisions.md` now carries one consolidated narrative entry for hold/release detection instead of scattered correction rows. Still open: focus instability on activation, now the largest remaining source of failed gestures.
- 2026-08-21: Step 3 closeout — **`Alt+Space` was silently broken and is replaced**. What began as removing a disproven 50ms settle buffer surfaced a total failure: 25 consecutive gestures lost the trigger key's `keyup`, including deliberate holds that the previous session recorded as verified. Isolating one variable at a time ruled out the buffer (the failure reproduced identically in the pre-edit configuration), then the Step 1 placeholder window stealing focus, and landed on the accelerator: macOS routes `Option+<key>` through the text-input/composition layer (Option+Space = U+00A0), which swallows the trigger key's events. `Control+Shift+D` → 8/11 finalized; `Control+Shift+Space` (isolating Option from Space) → 8/11 with zero missing keyups, proving Option was the culprit and Space was always fine. `uiohook-napi`'s revisit trigger appeared to fire and then didn't — the mechanism was never at fault. Also fixed in closeout: the stuck-gesture safety net was force-closing genuine in-progress holds at 5s (4 occurrences), now reworked into a liveness watchdog driven by auto-repeat keydowns. Current accelerator is verified-working but ergonomically poor and explicitly interim.
- 2026-08-20: Phase 1 Step 3 built and verified — Selection Overlay (transparent full-screen window, focus-grab, "Release to select" badge, freeform trail rendering, bbox accumulation, click-to-reset, min-size + edge clamping). Substantial debugging arc along the way: hotkey simplified to `Alt+Space`; the live bbox-preview rectangle was superseded by click-to-reset; the trigger key's `keyup` was found to be claimed by the still-registered accelerator (fixed via unregister-on-activate); a stuck-gesture safety net was added and later extended to reset the renderer as well as the main process. Quick tap-and-release remains intermittently unreliable and unresolved — bounded by the safety net; `uiohook-napi` re-weighed and deliberately deferred (see decisions.md).
- 2026-08-19: Phase 1 Steps 1-2 built and verified — Electron scaffold (`main.js`/`index.html`/`package.json`) launches a window; `globalShortcut` hotkey (`Cmd+Shift+Space`, press-to-toggle) shows/focuses it as a temporary overlay stand-in.