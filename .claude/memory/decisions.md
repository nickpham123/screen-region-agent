# Decisions

> **Read this first if you are touching the hotkey, the overlay, or any hold-to-X gesture:**
> [Hold/release detection on macOS + Electron — the whole mechanism](#holdrelease-detection-on-macos--electron) below the table.
> It is one consolidated explanation, written because this took a full session of
> debugging to work out and the table rows alone read as a pile of sequential
> corrections. Step 5's hold-to-talk inherits all of it.

Locked-in project decisions and why. Don't re-litigate these without the user explicitly revisiting them — if a future session finds itself reconsidering one of these, that's a signal to ask rather than silently change course.

| Decision | Rationale | Status |
|---|---|---|
| Phase 1 vision backend: **Mistral API only** (`mistral-small-latest`) | Free tier is the most generous of the options evaluated; fastest path to validating the product idea | Active |
| Local MLX backend (Apple Silicon) | Built and tested, then explicitly dropped in favor of Mistral-only | Dropped — don't reintroduce without being asked |
| Hugging Face Inference Providers backend | Built and tested, then explicitly dropped in favor of Mistral-only | Dropped — don't reintroduce without being asked |
| Fine-tune target: **Qwen2.5-VL-7B** via QLoRA/Unsloth | Best open VLM for OCR/grounding at a fine-tunable size; runs on a single GPU with QLoRA | Active, Phase 4 |
| Training compute: **UNL HCC Swan** (SLURM) | Free via user's university access; shared/preemptible, so checkpointing matters | Active, Phase 4 |
| Selection shape: **bounding box**, not freeform lasso | Simpler capture/masking logic for v1 | Active, v1 |
| Output: **popup by default**, TTS opt-in | Popup works even when audio can't be on | Active |
| Stable interface: `ask_about_region(image_path, question) -> str` | Keeps the client decoupled from any specific model/API — this is what makes backends swappable | Load-bearing, don't break |
| OS target for v1 client | **macOS** | Resolved — user develops on Apple Silicon Mac |
| App shell: **Electron** (not Tauri, not native Python/PyQt) | `globalShortcut`, `desktopCapturer`, and transparent `BrowserWindow` are all built-in, well-documented Electron APIs that map directly to this app's four core needs (hotkey, capture, overlay, popup). Tauri would need custom Rust glue for capture + permissions; PyQt/pynput/mss (original plan) meant a second language stack for no benefit once Electron was on the table. Tradeoff accepted: heavier runtime footprint (Wispr Flow's own Electron build has known performance complaints) in exchange for faster path to a validated prototype. Revisit only after the product is validated and performance is a proven, not hypothetical, problem. | Active |
| Vision backend language: **ported to JS**, not a Python subprocess | Electron is already a Node process; Mistral's API is plain HTTP. Shelling out to Python would add a cross-language IPC boundary for no real benefit — violates Simplicity First. `mistral_vision_query.py`'s logic and `ask_about_region()` contract carry over 1:1, just reimplemented in JS. | Active |
| Step 2 hotkey handler targets the Step 1 placeholder window, not a new one | Step 3 is what builds the real transparent overlay `BrowserWindow`; building overlay UI early would be scope creep on Step 2. The existing blank window stands in for it — hotkey calls `.show()`/`.focus()` on it, marked with a `// TEMPORARY` comment in `main.js` — and gets swapped for the real overlay when Step 3 lands. | Active, superseded once Step 3 lands |
| Hotkey interaction: **press-to-toggle** via `globalShortcut`, not true hold/release | `system_design_plan.md` §3.1 specs the Hotkey Listener as `onSelectionStart()`/`onSelectionEnd()` (hold-down, release-up), but Electron's `globalShortcut` API only fires once per completed accelerator press — no release event exists. True hold/release would need a raw-input hook like `uiohook-napi`, which was evaluated and rejected for now: it requires macOS's Accessibility permission (a separate, heavier onboarding prompt than screen recording), is documented to crash rather than degrade gracefully when that permission is missing, and grants full system-wide input monitoring — a much broader capability than this feature needs, which sits awkwardly next to the app's privacy-first framing. Going with press-to-toggle for Step 2; will validate whether focus-grab is reliable enough when Step 3's real overlay lands, and only revisit `uiohook-napi` if that assumption actually breaks in practice. | Active, open question re: revisiting at Step 3 |
| Question input: **both typed and voice**, voice via **local whisper.cpp** (`whisper-node-addon`) | The crop alone doesn't tell the model what the user wants to know — question is inherently free-form, so both input modes matter. **Correction**: Web Speech API was initially chosen, then found to be fundamentally broken in Electron (throws a persistent "network" error — Electron's Chromium lacks the proprietary Google API key the service needs; unresolved issue since 2016). Switched to `whisper-node-addon` — prebuilt Node/Electron bindings for whisper.cpp, no native compilation needed, Metal GPU acceleration on Apple Silicon. Bonus: this also fully resolves the earlier privacy tradeoff — audio never leaves the device, no opt-in caveat needed for Phase 7. | **Open again 2026-08-23 — see the row below.** Not dropped outright; the package genuinely doesn't work as published on this machine right now, which is different from "still fine, just a cautionary label" |
| **`whisper-node-addon`'s "early experimental" label investigated 2026-08-23 — confirmed to be more than a cautionary label** | Raised on review before writing any transcription code: proceed with the already-decided package despite its own README calling it "early experimental," or reconsider? Initial call (user, before installing anything): proceed — reopening a locked-in decision over a label alone, with no observed misbehavior, isn't this project's standard. Two conditions attached: check what happens on a failed native-addon load, and log the label as a tracked finding with a concrete revisit trigger rather than a vague asterisk. **What was actually found, empirically, meeting the user's own proposed revisit trigger ("fails to build on a clean install") on its first real test:** `npm install whisper-node-addon` succeeds and ships a prebuilt `darwin-arm64/whisper.node`, so no build/compile step runs for a normal install. But requiring it inside this project's real Electron process (not assumed from docs — tested directly, `diagnostics/verify_whisper_load.js`) shows the module itself loads fine (N-API's ABI stability holds across the Electron 31.7.7-built binary vs. this project's ^43.4.1, confirmed rather than assumed), **but calling `transcribe()` fails at `dlopen()`**: the binary's embed `@rpath` for its own `libwhisper.1.dylib` points at the original CI build machine's absolute path (`/Users/runner/work/whisper-node-addon/...`), which obviously doesn't exist on any consumer's machine — a real packaging defect in the published binary, not a local misconfiguration. Confirmed not fixable via the standard, non-invasive mechanisms: neither `DYLD_LIBRARY_PATH` nor `DYLD_FALLBACK_LIBRARY_PATH` pointed at the sibling `.dylib` directory resolved it (both tested directly, neither appears in the linker's own list of tried paths) — reading `dist/index.js` confirms there's no rpath-fixup or dylib-copy step in the package's own load path either, so this isn't something a documented workaround exists for. **The failure itself is graceful** — a cleanly rejected Promise with a readable error, process survives — so the user's first condition (does it crash hard) has a genuinely reassuring answer even though the package doesn't work. Separately, `npm audit` flags a critical `tar` vulnerability chain (`whisper-node-addon` → `cmake-js` → `tar`, several path-traversal/arbitrary-write CVEs) — checked whether this is actually reachable: `cmake-js`/`tar` are only invoked by the package's own `build:*` scripts (source compilation), never by a normal `npm install` or by `require()`ing the prebuilt binary, so it's inert dead weight in the tree for our actual usage, not a live exploit path — worth recording as checked, not left as an unresolved audit warning. **Not yet tried, deliberately, pending the user's input rather than unilaterally spending more effort**: patching the binary's rpath locally (`install_name_tool`, needs Xcode CLI tools, fragile — wiped by any `npm install`/reinstall unless scripted, and unproven to fully work even if the dlopen step is fixed) or building from source ourselves (exercises the vulnerable `tar` chain locally, one-time, and might just reproduce the same rpath bug since it's the same build process). **Worth noting**: the package's own README recommends `nodejs-whisper` as the mature alternative for production use — not evaluated yet, but a concrete, already-pointed-to option if this one is abandoned | **Superseded 2026-08-23 same day — see the row below.** `whisper-node-addon` uninstalled entirely (`npm uninstall`), which also cleared the `npm audit` critical finding to zero, confirming it was solely attributable to this package |
| **Switched to `nodejs-whisper`, replacing `whisper-node-addon`** | Evaluated per the user's explicit ask, checking the two properties that justified the original choice rather than assuming a backend swap preserves them for free. **Architecturally different, worth being precise about**: `nodejs-whisper` shells out to a compiled `whisper-cli` binary as a child process (via `shelljs`), not an in-process native addon — confirmed by reading `dist/whisper.js` directly, not inferred. This sidesteps the whole Electron-ABI-matching question that mattered for the native-addon approach, since a subprocess just needs to match the OS/architecture, not any particular Node/Electron build. No prebuilt binary ships with the package — it compiles whisper.cpp from source via CMake on first use (`node_modules/nodejs-whisper/cpp/whisper.cpp`), requiring `cmake` (installed via `brew install cmake` — wasn't present, noted here rather than done silently; reversible with `brew uninstall cmake`) and Xcode CLI tools (already present). **Both re-verified properties, empirically, via a full real build + real transcription (`diagnostics/verify_nodejswhisper.js`), not from docs**: (1) **on-device** — the only network access observed was the one-time model download (`base.en`, ~141MB from Hugging Face, ~6s); actual `whisper-cli` execution and transcription showed zero further network activity; (2) **Metal** — genuinely uncertain from docs alone (`nodejs-whisper`'s own README says "Optimized for CPU (Including Apple Silicon ARM)," which reads as CPU-only and does NOT confirm GPU use), so this was directly tested rather than trusted either way. The real CMake configure output settled it unambiguously: `-- Metal framework found` / `-- Including METAL backend`, and the real transcription run's own logs confirm Metal was actually *engaged*, not just compiled in and unused: `ggml_metal_device_init: GPU name: MTL0 (Apple M1 Pro)`, `whisper_backend_init_gpu: using MTL0 backend`. Whisper.cpp's own CMake defaults to Metal on Apple Silicon with no special flag needed, and `nodejs-whisper`'s build command is a bare `cmake -B build` (read from `dist/buildConfig.js`) — so this isn't a one-off, it's the package's normal behavior on this class of machine. **Also measured**: transcription accuracy (a synthesized "Testing the region agent voice input." came back verbatim, only capitalization differed) and speed (775ms total whisper processing for a 2.46s clip once already built — load 121ms + encode 409ms + decode 159ms; the 91.8s the full diagnostic run reported was almost entirely the one-time source compile, confirmed one-time by the log's own "whisper-cli executable found. Skipping build." path on the actual transcription call). **Not yet done**: wiring this into the app itself (recording audio in the Chat Panel's renderer via `getUserMedia`, saving it to a temp WAV file, calling `nodewhisper()` from the main process, populating the text field) — this row is the backend evaluation only. `whisper-node-addon` uninstalled; `nodejs-whisper` is now the sole dependency beyond `electron`. **Packaging robustness, checked explicitly rather than assumed carried over**: structurally can't hit `whisper-node-addon`'s exact failure (a broken baked-in path from a prebuilt binary), because it ships no prebuilt binary at all — confirmed empirically, not just architecturally, since the real build-and-run pipeline completed with zero dlopen/path errors. `npm audit` is clean for it specifically (0 vulnerabilities once `whisper-node-addon` was removed). **But it trades that fragility for a different one, worth flagging now rather than at Phase 7**: it depends on a local C++ toolchain (Xcode CLI tools + `cmake`) being present to compile whisper.cpp on first use — fine on a dev machine, but a packaged app's end user won't have one, so this would fail outright for a real user the way the original package's broken binary did, just differently shaped. Tracked in todo.md's Later/deferred section as a Phase 7 packaging task (bundle a binary we build ourselves, don't compile on the user's machine), not blocking now | Active — implemented as a dependency 2026-08-23, wired into the app the same day (see the row below) |
| **Hold-to-talk voice recording implemented, per the design walked through and approved before writing it** | `chatPanel.html` captures raw Float32 PCM via a `ScriptProcessorNode` (chosen over `AudioWorkletNode` — no separate module file to load, less machinery for a Phase 1 prototype; noted as a deliberate simplification, not a default) while `holding` is true, routed through a zero-gain node to keep the processor callback firing without audibly looping the mic back to the speakers (a known engine quirk, not a design choice). Renderer hands the raw buffer to `main.js` on the real keyup — no processing on that side, same raw-data-out split `capture.js` already established for screenshots. `main.js` builds a 16kHz mono WAV by hand (`floatTo16BitWav()`) rather than relying on `nodejs-whisper`'s own ffmpeg-based conversion — `ffmpeg` isn't installed, and adding it would stack a second Phase 7 packaging dependency on top of the one `whisper-cli` itself already needs (see the row above and todo.md's blocking checklist item). **Minimum-duration guard, per the user's explicit refinement**: keyed on actual captured audio length (`MIN_VOICE_DURATION_MS = 200`), not raw press-to-release hold time — the two aren't the same thing, and a genuine short utterance ("yes", "stop") can involve a short hold that still contains real speech, so gating on hold-duration risked silently discarding real speech, worse than the accidental-tap problem the guard exists to solve. **Explicit, visible feedback for every non-happy-path**, also per the user's explicit ask, not left silent: mic-unavailable, too-short, and transcription-error each flash a distinct message in the recording indicator (`flashRecordingStatus()`) rather than nothing happening — consistent with this project's standing rule (the badge fix, the concurrent-session guard) that a blocked action must never read as inert. `extractTranscriptText()`'s string-vs-`string[][]` ambiguity (see the row above) was resolved with a direct empirical check before relying on either branch: `typeof`/`Array.isArray` against a real `nodewhisper()` call confirmed it returns a plain timestamped string, not the array shape its own `.d.ts` claims; the string branch is what real calls take, the array branch is kept as cheap unexercised insurance. **Both open questions confirmed hands-on, 2026-08-23**: (1) `AudioContext`'s requested `sampleRate: 16000` is honored exactly by real mic hardware (`AudioContext sampleRate=16000 (requested 16000)`, logged on every real hold across two separate test sessions) — the ffmpeg-avoidance plan holds, no resampling needed. (2) Real transcriptions land correctly and accurately: 5 real dictations across two sessions, all ended by real keyup (not the watchdog), all transcribed with only trivial capitalization/punctuation differences from what was actually said. **First test session surfaced two follow-on issues, both since fixed**: (a) the very first-ever hold (also the app's first `getUserMedia()` call) hit the watchdog's 5s timeout instead of ending on a real keyup — a single, unreproduced anomaly. One hypothesis consistent with it is the already-tracked missing-`blur`-listener gap (todo.md), on the theory that a real macOS mic permission dialog appeared and stole focus mid-hold, but this is **not confirmed**: `isFocused()` was never instrumented during that attempt, whether a dialog actually appeared was never checked, and the event has not recurred across 8 subsequent attempts. Recorded as an open, unreproduced anomaly, not a settled finding — the existing safety net handled it correctly regardless of cause. (b) Multiple hold-to-talk segments in one session were replacing the field's content instead of accumulating — fixed by appending (trimmed, single-space-joined) instead of overwriting, with the cursor explicitly moved to the end. **Second test session surfaced a further real UX gap this fix exposed**: appending was confirmed working via inspection (pressing Home revealed the earlier text was there, just scrolled out of view), but a single-line `<input>` only ever shows the tail of a multi-segment message as it grows — confirmed by the user hitting this directly while dictating a longer real question. Fixed by switching `#input` from a single-line `<input>` to an auto-growing `<textarea>` (grows with content up to 140px, then scrolls internally), with the standard chat convention added alongside it (`Shift+Enter` for a literal newline, plain `Enter` still submits). Re-verified after the textarea fix: 2 more real multi-segment dictations, confirmed by the user to accumulate and stay visible correctly this time | Active — implemented and hands-on verified 2026-08-23 |
| Hold-to-talk key: **same physical key as region selection**, held again once the chat panel has focus | Superseded a prior, overly-cautious call to use a mouse-held mic button instead. Correction: `globalShortcut`'s missing-keyup limitation only applies while no Electron window has focus. Both the selection overlay and the chat panel take focus immediately on activation, so from that point on, the same key is tracked via normal window-scoped `keydown`/`keyup` — release detection works fine. One key, one mental model, for both the select-hold and the talk-hold. | **Superseded 2026-08-23 — the "only applies while no window has focus" claim is false, confirmed empirically, not theorized.** See the "Hold-to-talk vs. the accelerator" row below for the mechanism (chord-timing order, not focus state) and its resolution. "One key, one mental model" is still true at the UX level — the design decided here didn't change — but the technical claim that justified skipping any special handling for it did not hold up |
| Region selection: **hold the key through the whole drag**, not press-once-then-mouse-only | Superseded an earlier, more conservative design (press once to open overlay, mouse handles everything after). Same reasoning as above — the overlay takes focus on activation, so the hold-and-release gesture works via window-scoped events, matching the Wispr Flow-style "hold the whole time" feel more faithfully. | Active |
| Voice transcription: **record-then-transcribe-on-release**, not live streaming | whisper.cpp doesn't naturally support word-by-word live transcription — that requires a rolling-window re-transcription approach with real complexity and flicker tradeoffs. Simpler and sufficient for v1: buffer audio while the key is held, transcribe the full clip in one pass on release. | Active |
| Conversation model: **real multi-turn, scoped to one open chat panel session** | "Like a regular chatbot conversation" — follow-ups need the model to remember prior turns in the same session, so the `askAboutRegion()` contract now carries conversation history, not just one question. Explicitly NOT persisted or resumable — once the panel closes, the conversation is over; the full turn history is logged once at close, for fine-tuning data, not for browsing past chats later. Cost tradeoff accepted: longer sessions cost more tokens per follow-up than independent single-shot calls would. | Active — changes the data schema, see project_state.md/data_pipeline.md |
| Marketing/dashboard website (cluely.com-inspired) | User wants a separate site introducing the product, for later — not blocking current Phase 1 work. | Noted, deferred — not yet scoped into a phase |
| Session length: **no artificial limit** — full history held for as long as the panel is open | `mistral-small-latest` has a 262K-token context window; a realistic Chat Panel session would need dozens of turns to approach it, so a soft cap or history-trimming isn't worth engineering for now. The cost tradeoff already logged (every turn resends full history + image) still applies — this decision means it's accepted as-is, not that it's been eliminated | Active — resolves the open question in system_design_plan.md §9 |
| Selection shape, resolved: **freeform gesture, bounding-box capture** | User draws whatever shape feels natural (matches "circling" something); on release, only the bounding box of the path is captured, never a true mask. Vision models tokenize images as a rectangular patch grid, so an irregular mask wouldn't help the model and would only add real engineering complexity (canvas clipping, alpha handling) for no accuracy benefit. This gets the UX win with none of the downside — supersedes the earlier "bounding-box for v1, lasso in v2" split | Active — resolves system_design_plan.md §9's lasso question |
| Live bounding-box preview during the drag | Freeform paths can produce a much bigger box than the user intended (an elongated scribble can bound-box into most of the screen). Rendering the current bbox continuously during the drag lets the user self-correct in real time, rather than needing rejection/warning logic after the fact | **Superseded** by click-to-reset (below) — see that row for why the answer changed, reasoning kept here for the record |
| Full-screenshot context, resolved: **hybrid** — cheap downscaled thumbnail every call + a tool to escalate to full-resolution when needed | Pure tool-calling (considered first) depends on the model reliably recognizing it needs more context — smaller/cheaper models like Mistral Small aren't reliably self-aware about this, more likely to guess than ask. A cheap always-sent thumbnail removes that failure mode for the common case, since image tokens scale with resolution and a small thumbnail costs very little; the escalation tool keeps the expensive full-resolution path rare rather than eliminating it entirely | Active — resolves system_design_plan.md §9's image_context question |
| API key location, resolved: **local env var through Phase 1–6, Supabase Edge Function environment at Phase 7** | A packaged app can always be inspected by whoever's running it — Electron especially, the JS sits in an unpacked `.asar` — so anything embedded in the client bundle isn't actually secret once someone besides the developer has a copy. Fine to keep local (env var, or Electron's `safeStorage` for extra polish) while only the developer has the app; must move server-side, never shipped to the client, at the exact point the app is first distributed to anyone else | Active — resolves the last open question in system_design_plan.md §9 |
| Selection overlay: **"ready to select" indicator** on activation | No feedback currently exists confirming the hotkey registered before the drag starts — a small outlined/translucent badge ("Release to select") fills that gap, fades once dragging begins | Active |
| Fine-tuning goal, made explicit: **visual reading accuracy + response quality**, not conversational polish | Shapes what Phase 5's eval scoring actually optimizes for — a fluent answer that misreads the image is a failure, a terse correct one is a win | Active — affects eval criteria in implementation_plan.md Phase 5 |
| Phase 7 database/backend: **Supabase** (Postgres + Auth + Storage + Edge Functions) | Fully managed — no separate FastAPI service to deploy/maintain for Tier 1, Edge Functions handle the thin routing layer directly against Supabase's own Postgres | Active — supersedes the earlier generic "FastAPI backend" sketch in implementation_plan.md Phase 7 |
| Current database status: **none** | Phase 1–6 storage is local-only (SQLite/JSONL on the user's own machine, for the fine-tuning dataset) — not a real database with user accounts. A real (Supabase) database only becomes necessary at Phase 7 | Active, correctly reflects current state |
| Hold-gesture release detection, general principle: track the **non-modifier trigger key** of the chord, never the modifiers | Established concretely for Step 3 (track `Space`'s `keyup`, not `Cmd`/`Shift`, from `CommandOrControl+Shift+Space`) — modifier keys don't reliably fire clean `keyup` events across input contexts. Stated as a general rule, not a one-off, because it applies again to the Chat Panel's hold-to-talk gesture (same physical key, same window-scoped mechanism per system_design_plan.md §3.1/§3.4) | Active |
| Selection Overlay: **primary display only** for v1 | Multi-monitor support means enumerating `screen.getAllDisplays()` and handling heterogeneous origins/scale factors — real, isolated complexity not requested anywhere in current scope. Deferring costs little since it's a bounded future change (loop over displays, one overlay window per display), not an architecture rewrite. Explicit, temporary limitation — an external monitor silently won't work with the overlay until this is revisited | **Superseded 2026-08-21 — see the row below.** The prediction held, but the word "silently" turned out to be the real cost: with a second monitor attached the hotkey fired, logs looked healthy, and the overlay rendered on an unreachable screen — indistinguishable from the app being broken, and it cost a diagnosis cycle to identify |
| **Product requirement: the overlay follows the cursor's display** | The app must work on whichever screen the cursor is on at activation, regardless of where the Electron app window lives or which window last held OS keyboard focus. **Cursor position is the tracked signal, not frontmost-window/OS focus** — confirmed explicitly, not assumed. Reasoning: the core gesture is a mouse drag, so cursor position is the direct answer to "which screen is the user about to act on", more direct than window focus and implementable with one built-in Electron call (`screen.getDisplayNearestPoint`) rather than macOS Accessibility APIs, which would add a permission prompt and real plumbing for no benefit to this interaction model. Implementation: `overlayWindow.setBounds(display.bounds)` before `show()`, per activation. **Scope**: still a *single* overlay window, repositioned each time — not one overlay rendered per display simultaneously, which stays deferred. **Accepted tradeoff**: this reuses `getCursorScreenPoint()`, measured elsewhere to return a ~29px-stale position while the cursor is moving fast (see the activation-trail bug). Considered and accepted rather than overlooked — display selection only misfires if the cursor crosses a display boundary within a few ms of the keypress, and the consequence is a one-off overlay on the neighbouring screen, not a wrong crop. **Verified 2026-08-21** on a built-in Retina (1728pt logical) + external QHD (2560×1440) setup: gestures finalize on both displays, corroborated in-log by bboxes extending to x=2297, impossible on the built-in. The disambiguating case — cursor on display B while a window on display A retained focus, no click in between — was run and passed, confirming cursor position drives the choice rather than window focus; note that condition rests on direct observation, since the log records no focus state | Active — supersedes the row above |
| Hold-gesture "is the key currently down" state: **bootstrap from the activation signal, not from a local `keydown`** | A key already physically held when a window gains OS focus generally does not fire a `keydown` for that key on focus-gain — only the eventual `keyup` reliably arrives. Discovered debugging Step 3's overlay (the hotkey handler fires `app.focus({steal:true})` + shows the window while `Space` is already down as part of the triggering chord, so a window-scoped `keydown` for `Space` can't be relied on to ever arrive). Fix: treat the activation signal itself (here, the `activate` IPC message, sent only because the chord — including the trigger key — was just pressed) as proof the key is currently held, and set the "held" flag from that directly; a local `keydown` listener can still exist defensively but isn't load-bearing. Only the `keyup` is what reliably ends the gesture. Applies wherever a hold gesture starts via a focus transfer, not just here — will recur at Step 5's Chat Panel hold-to-talk (same physical key, same window-scoped mechanism per system_design_plan.md §3.1/§3.4) | Active — apply proactively at Step 5, don't rediscover |
| Hotkey accelerator simplified: **`Alt+Space`** (Option+Space), replacing the 3-key `CommandOrControl+Shift+Space` | Hands-on testing surfaced the 3-key chord as genuinely awkward to hold one-handed while the other hand drives the mouse. Checked against macOS's own default global shortcuts: `Cmd+Space` (Spotlight), `Ctrl+Space` (input source switch), and `Ctrl+Cmd+Space` (emoji viewer) are all reserved; `Alt+Space` isn't a macOS default (and is a common Spotlight-replacement binding in apps like Raycast/Alfred, a decent signal it's not a common collision either). Can't rule out a conflict with some third-party app already on the user's machine — only OS defaults were checkable here. **Flagged for later**: this should become user-configurable eventually; explicitly not in scope now | **Superseded 2026-08-21** — `Alt+Space` was silently broken, see the root-cause row below. The rationale here had a specific gap worth learning from: it checked the accelerator against macOS's reserved *global shortcuts* and stopped there. That check can't surface the actual problem, which lives one layer down in text input, not in the shortcut registry |
| Hotkey **unregister-on-activate, re-register-on-finalize** — confirmed fix for deliberate hold-drag-release | While the accelerator stays registered, macOS's global-hotkey layer claims the trigger key's `keyup` exclusively — it never dispatches as an ordinary window-scoped event, even to a window that holds real focus. Confirmed by diagnostic logging: before the fix, `AltLeft`'s keyup arrived every time while `Space`'s never did; after it, `Space`'s keyup arrives and gestures resolve. `globalShortcut.unregister()` in the activation callback releases that claim; `registerHotkey()` on finalize re-arms it. **Scope of this claim**: verified reliable for deliberate press-hold-drag-release gestures. Does NOT cover quick taps — see the row below | Active, but **its evidence base is now suspect (2026-08-21)** — the diagnostic that "confirmed" it (`AltLeft`'s keyup arriving while `Space`'s never did) was collected on `Alt+Space`, and that exact asymmetry is now explained by Option's text-input interception rather than by the accelerator holding a claim. The unregister call is still in the code and gestures work with it, so nothing is being changed on a guess — but whether it's actually load-bearing is untested on a non-Option accelerator. Cheap to settle: remove it, run the gesture protocol, see if keyups still arrive |
| **Unresolved**: quick tap-and-release intermittently loses keyboard events entirely, on a window reporting `isFocused(): true` | Distinct from the row above, and not fixed by it. On fast press-release cycles the overlay sometimes receives no `keyup` at all — at minimum `Space`'s, and in at least one confirmed instance *both* `Space` and `AltLeft` went missing in the same gesture, with `overlayWindow.isFocused()` logging `true` throughout. This is broader than the original "the accelerator claims one specific key" theory: it's keyboard events not reaching a window that reports having focus. An `UNREGISTER_SETTLE_MS = 50` buffer between `unregister()` and activating the overlay was tried on the hypothesis that the OS event pipeline hadn't caught up — **it did not fix it** (log shows the settle window fully elapsed, then the release still never arrived). Root cause still unknown | **Largely resolved 2026-08-21, downgraded from "the uiohook trigger"** — the sweeping part of this claim ("keyboard events don't reach a window reporting focus") was an artifact of the Option+Space interception, not a general Electron/macOS focus-delivery defect: on a non-Option accelerator, keyups arrive reliably and 16/22 gestures finalized. What genuinely remains is narrower but **confirmed accelerator-independent (2026-08-21)**: quick taps lose the trigger key's events on `Control+Shift+D`, and again on `Shift+Escape`, which contains no Option — so switching accelerators never addressed this and never could. Signature is consistent every time: activation fires, the overlay receives at most one `mousemove`, then no auto-repeat `keydown` and no `keyup` at all, and the watchdog force-closes ~5s later. Most likely the key is released before the overlay wins focus (focus lands ~50-100ms after activation), i.e. a race rather than lost events, but that's still unconfirmed. Risk profile unchanged and still low: a tap that fast yields a below-minimum-size selection that gets discarded anyway, and the watchdog bounds the cleanup. **Open, lower priority, and explicitly NOT the trigger for reconsidering `uiohook-napi`** — that trigger is deliberate holds losing keyups, which is not happening |
| Stuck-gesture **safety net** (`STUCK_GESTURE_TIMEOUT_MS`, currently 5s) — a mitigation, explicitly not a fix | If a gesture activates but never resolves, a main-process timer force-closes it: cancels the renderer's gesture state, hides the overlay, re-registers the hotkey. Verified working, including the renderer-reset half (`[safety-net]` → `[renderer] gesture cancelled` pairing in the log). **Two distinct claims worth keeping separate**: (1) the app is now guaranteed not to hang permanently or strand the hotkey unregistered — that's real and verified; (2) the underlying event-delivery failure above is unfixed — the safety net only bounds its damage. An earlier version of this net hid the window without notifying the renderer, which left `spaceHeld` true and the trail drawn, so movement kept drawing an already-dead gesture; both processes are now reset | Active — mitigation only, don't mistake for a fix |
| `uiohook-napi` re-weighed against confirmed evidence — **still not adopted**, but for updated reasons | The original rejection (see the "press-to-toggle" row) was made against a *hypothetical* reliability risk. It's now been re-evaluated against three confirmed `globalShortcut` failures. **What changed in its favor**: (1) it hooks the OS input layer (`CGEventTap`), *below* window focus routing — since the confirmed failure is a focus-routing failure, code not depending on focus routing can't fail that way, so it targets the actual failing layer rather than symptoms; (2) the "crashes without Accessibility permission" objection is largely obsolete — that was SnosMe/uiohook-napi issue #24, fixed via PR #51 (verify the pulled version postdates it); (3) the permission-cost objection is weaker than assumed, since Step 4 already requires a Screen Recording prompt, making this the second prompt rather than the first; `systemPreferences.isTrustedAccessibilityClient()` (confirmed current Electron API) allows checking before initializing, with graceful fallback — though the permission still needs explaining to the user, not silently failing. **What still argues against**: full system-wide keystroke visibility remains a real capability expansion for a privacy-first app — the objection with the most weight remaining; plus a native dependency adds Electron-version coupling and Phase 7 signing/notarization friction. **Why not now**: the failure only manifests on quick taps, which produce a below-minimum-size selection that's discarded anyway — no useful gesture is being lost; the safety net bounds it to a ~5s cosmetic lingering overlay with correct state reset; and the actual product gesture (deliberate hold-drag-release) is verified reliable. Also assessed: Step 5's hold-to-talk does *not* obviously inherit this risk — it has no accelerator registered and the panel already holds focus, so it's the ordinary keydown/keyup case, not the exotic activation path where every confirmed failure occurred. **Explicit triggers to revisit**: deliberate holds (not just quick taps) start losing keyups, or Step 5's hold-to-talk hits the same failure class. Either would mean the primary input mechanism is unreliable — a different decision entirely | Deferred, not dropped — **trigger 1 appeared to fire on 2026-08-21 and then did not**: deliberate holds lost keyups across 25 straight gestures, which looks exactly like the stated trigger, but the cause was a bad accelerator (Option), not a `globalShortcut` limitation. Fixed by changing the hotkey — no dependency needed. The lesson for next time this trigger seems to fire: **rule out the accelerator before concluding the mechanism is at fault**, since a trigger-key-specific OS interception is indistinguishable from "globalShortcut can't do hold/release" from the symptoms alone |
| Stuck-gesture timeout stays at **5s**, not tightened to ~4s | A shorter timeout was proposed to make discarded quick taps less noticeable, with a ~4s floor computed from observed deliberate-hold durations (2.5–3.5s). Rejected: that sample is a handful of holds by one person in a testing mindset — real usage (a careful irregular trace, a first-time user, anyone slower than a developer running verification passes) could plausibly exceed it. Truncating a genuine in-progress selection with no explanation is a much worse failure than the current cosmetic cost of a discarded tap lingering 5s. Not worth the trade on this little data | **Superseded 2026-08-21** by the liveness-watchdog row above — and the concern recorded here turned out to be well-founded and *already happening at 5s*, not just at 4s: real holds were truncated mid-draw 4 times across runs C and D. Notably, the "2.5–3.5s deliberate hold" sample this row rightly distrusted was indeed unrepresentative — observed holds reached 4.5s+ once the accelerator actually worked. The watchdog removes the tradeoff rather than re-tuning it, so no constant needs picking |
| **Root cause of the `Alt+Space` failure: never use Option (`Alt`) in a hold-gesture accelerator on macOS** | Option is macOS's special-character modifier — `Option+Space` emits a non-breaking space (U+00A0), `Option+D` emits `∂`, and so on. Any `Option+<key>` chord is routed through the text-input/composition layer, which consumes the trigger key's raw `keydown`/`keyup` before they reach a focused window. Measured: across **25 consecutive gestures** on `Alt+Space`, not one `Space` event of either kind reached the overlay — while `AltLeft`'s keyup arrived normally in the same gestures, on a window reporting `isFocused(): true`. Switching to `Control+Shift+D` gave 8/11 finalized; isolating the variable with `Control+Shift+Space` (Space back, Option gone) gave 8/11 with **zero** missing keyups, proving Space was never the problem — Option was. Generalizes past this one chord: the reserved-global-shortcut check that picked `Alt+Space` cannot detect this class of failure, so any candidate accelerator must be **empirically verified** to deliver its trigger key's keyup, not just checked against a list. **Scope limit — read this before citing the row**: it explains the `Space`-missing/`AltLeft`-arriving asymmetry on `Alt+Space` and the total failure of deliberate holds there. It does **not** explain quick-tap keyup loss, which persists unchanged on `Shift+Escape` (no Option involved) — that's a separate, accelerator-independent cause that no accelerator change has ever targeted. Do not read this row as "quick taps are fixed" | Active — verified 2026-08-21 |
| Hotkey accelerator, **resolved: `Control+1`** | Chosen after testing seven candidates hands-on (2026-08-21). Measured 11/11 activations, `Digit1`'s keyup delivered every time, zero force-closes. Two keys, comfortable left-hand reach, and `Ctrl+<digit>` is free on macOS because apps bind `Cmd+<digit>` for tab switching. Supersedes the `Control+Shift+Space` interim row below | Active |
| **Registration success proves nothing** — every accelerator must be hands-on verified | Five candidates registered cleanly via `globalShortcut.register()` and then failed, each in a *different* way: `Alt+Space` (Option → text-input/composition layer eats the trigger key), `F6` (macOS consumes bare F-keys for hardware functions; callback never fires), `` Control+` `` (registers, callback never fires — accelerator string seems not to map to the physical key), `Control+Space` (fires, but macOS's input-source switcher grabs the chord too — visible as an `IMKCFRunLoopWakeUpReliable` error — and swallows Space's events), and the three-key chords (work fine, just ergonomically poor). No static check catches any of these | Active — treat as a hard rule when picking any future accelerator |
| **Input-method interception is a recurring failure class**, not a one-off | `Alt+Space` and `Control+Space` failed the same underlying way: a macOS input-method layer sits between the OS and the window and consumes the trigger key's raw events. Option is the special-character modifier; `Control+Space` is the input-source switcher. Symptom in both cases is identical and misleading — the accelerator fires, the window has focus, and no key events ever arrive. **Check this first** when a new accelerator misbehaves, before suspecting focus, `globalShortcut`, or the code | Active |
| **Focus instability on activation was a real defect** — recorded 2026-08-21 as "the largest remaining source of failed gestures" | Measured `isFocused()` flipping within single gestures (`true`→`false` at +200ms, and `false`→`true`), with no keyboard events arriving while mouse events kept flowing. Intended fix was to wait on the window's `focus` event before arming | **Superseded same day — see the row below.** Kept per this file's own rule: the original claim stays visible rather than being quietly overwritten, because the *way* it was wrong is the useful part |
| **Correction: focus instability was a testing artifact, not a defect** | The measurements above were taken while several Electron instances were running concurrently — each debugging round launched `npm start` without terminating the previous app, and every instance registered the same accelerator and called `app.focus({steal:true})` on its own overlay. Confirmed directly: one physical keypress at `04:48:33` is logged by **five separate instances** within 2ms, reproduced again at `02:38:05` (see §4 for raw lines). **Separate the observation from the explanation**: that all five instances fired is measured fact; *why* is inferred, not documented. Electron's `globalShortcut` docs cover only the "already taken by **other applications**" case (documented as silently failing) and say nothing about multiple instances of the same app. On macOS even that documented contract looks unreliable — electron#21975 reports `register()` succeeding and the shortcut firing anyway rather than failing silently, which fits our five `true` returns. Underlying API is Carbon `RegisterEventHotKey`; no documentation found on how it arbitrates multiple registrants. Do not cite "macOS dispatches to every registered process" as established behavior. Clean single-instance measurement: `isFocused` `true` on **32 of 33** gestures vs `false` on **6 of 8** under contamination. Lasting lesson is methodological — a global-shortcut app is unusually vulnerable to stray instances, and `register()` returns `true` for all of them with no duplicate-registration warning | Active — supersedes the row above |
| **A window moved to a secondary display needs `setVisibleOnAllWorkspaces(true)` to receive keyboard focus** | **Separate the observation from the explanation.** *Observed and reproduced* (5 of 5 consecutive gestures, run `bkr82686s` 19:48): with the overlay repositioned onto the secondary display, it rendered there and received **mouse** events normally — the trail drew under the cursor — while **zero** trigger-key events arrived, so every gesture died at the 2.5s no-key-event deadline mid-hold. *Inferred, not measured*: that the cause is macOS running a separate Space per display by default ("Displays have separate Spaces"), with `app.focus({steal:true})` granting key status to a window on the **currently active Space** — the other display's, where the Step 1 placeholder window still lives. **`isFocused` was not instrumented during the failing run**, so the focus state was never directly observed; the explanation is the best fit for "mouse arrives, keyboard doesn't, on a relocated window", and the fix derived from it eliminated the failure, but it has not been proven. To convert it to measured: instrument `isFocused`, revert the one-line fix, reproduce. Symptom was maximally misleading: the trail drew normally under the cursor and then died at the 2.5s no-trigger-key-event deadline while the key was still held, which reads as a hotkey bug rather than a focus one. Fix is one line at window creation: `overlayWindow.setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true })`. **Note this is a second, separate change from the `setBounds` display-following fix, despite both landing in commit `5387246`** — different function, different lifecycle point (once at creation vs. per activation). Relocating the window's bounds does **not** fix Space assignment as a side effect, and that is established by experiment rather than argument: run `bkr82686s` had `setBounds` only and finalized **0 of 5** gestures; run `b61lzcxkk` added this single line and finalized **4 of 5**, including 10.4s and 6.3s holds. **Generalises**: any window this app shows on a display other than the active one needs the same treatment — relevant to Step 5's Chat Panel, which opens after capture and must take keyboard focus for typing. Also note the second, untried candidate fix — removing the leftover Step 1 placeholder window to eliminate the competing focus target — remains available if this recurs | Active — verified on a two-display setup 2026-08-21 |
| **Chord timing: the modifier must go down *before* the trigger key** | macOS only matches a global accelerator when the modifier flags are already set, so pressing both keys simultaneously frequently fails to activate at all. Discovered by the user after repeated "I have to press it multiple times" reports — which had been wrongly attributed to Escape's key actuation (a Touch Bar hardware theory that was wrong). This is a property of OS-level hotkey matching and applies to **every** chorded accelerator, not to any particular one. Two consequences: (1) it silently disqualified candidates during testing that were actually fine — `Control+Space` "never fired" until pressed modifier-first; (2) it's a real usability cost of chords generally, and a genuinely *new* argument for `uiohook-napi`, which does its own chord detection on raw key events and wouldn't care about press order. Not sufficient to reopen that decision, but it is a new input to it that the earlier weighings never considered | Active — note for Step 5, whose hold-to-talk uses the same chord |
| Hotkey accelerator, current: **`Control+Shift+Space`** — verified-working but explicitly interim | **Superseded 2026-08-21 by `Control+1`** (row above). | Chosen only because it isolated the Option variable and is proven to deliver Space's keyup. It reintroduces the exact ergonomic problem that got `Cmd+Shift+Space` replaced in the first place — three keys is awkward to hold one-handed while the other hand drives the mouse. A 2-key non-Option replacement is an open task; whatever is picked must clear both bars (not OS-reserved **and** empirically verified keyup delivery) | Active, interim — see todo.md |
| Accelerator configurability **promoted from "eventually" to a near-term open question** | This is the third hardcoded accelerator in three iterations (`Cmd+Shift+Space` → `Alt+Space` → `Control+Shift+Space`), each replaced for a reason the previous round didn't anticipate — ergonomics, then an OS input-layer conflict invisible to the checks being used. Continuing to hunt for one perfect global default is showing diminishing returns, and no hardcoded choice can account for third-party apps already bound on a given user's machine. Not scoping the work here, just recording that the evidence now favours making it configurable over finding a better constant | Open question — decide before Phase 7 packaging at the latest |
| Stuck-gesture safety net reworked from a **fixed deadline** into a **liveness watchdog** | The original fired 5s after activation regardless of what the user was doing, so it force-closed genuine in-progress holds mid-draw — observed 4 times across runs C and D (auto-repeat `keydown`s still streaming at the instant it fired). That's the failure the "keep 5s, don't tighten to 4s" decision was specifically trying to avoid, happening at 5s. Fix: a held key auto-repeats `keydown` every ~80ms; the renderer forwards those as a `gesture-alive` ping and the timer re-arms on each, so the timeout now measures **silence**, not elapsed duration. An active hold can't be truncated at any length; a genuinely stuck gesture is still force-closed, so the original guarantee (no permanent hang, no hotkey stranded unregistered) is unchanged. Strictly better than raising the constant, which would only have moved the truncation threshold. **Known limitation**: with macOS key repeat set to Off, no pings arrive and this degrades to the old fixed-deadline behavior | Active — supersedes the "timeout stays at 5s" row below |
| Click-to-reset: a plain click while the hotkey is still held clears the current trail/bbox tracking and starts fresh from the click point | Makes redrawing a selection cheap mid-gesture without needing to release and re-press the hotkey. Directly enables superseding the live bbox-preview rectangle (above) — once a bad/oversized path is one click away from being discarded, the always-visible box that existed specifically to help the user self-correct sprawl in real time is a redundant safety net. Whatever's accumulated since the last reset (initial activation or most recent click) is what finalizes on key release. A visible flash cue at the click point confirms the reset happened, since it would otherwise be silent | Active |
| Step 4 crop coordinate math: **full-resolution capture + convert bbox by `display.scaleFactor` once, inside `captureRegion(bbox)`** | The overlay's finalized bbox is in logical/DIP pixels (renderer `clientX/clientY`, relative to a window sized from `display.bounds`, itself logical); `desktopCapturer`'s real capture path is believed to return physical/backing-resolution frames. Three options weighed: (1) request a `desktopCapturer` thumbnail sized to `display.bounds` so no conversion is needed at all — rejected, thumbnails are a lower-fidelity preview path, a bad trade against the fine-tuning goal of visual reading accuracy; (2) capture full-resolution, multiply the finalized bbox by `scaleFactor` once at crop time — chosen; (3) convert per-mousemove-sample instead of once at the end — rejected as a relocation of (2)'s identical formula (scaleFactor is constant for a gesture's duration, so per-sample vs. once-at-the-end are equivalent modulo rounding order), not a real alternative, and it does more work per event for no new guarantee. Conversion lives entirely inside `captureRegion(bbox)` per system_design_plan.md §3.3's interface boundary, not smeared into gesture-tracking code | Active — scaleFactor formula verified 2026-08-22, see the diagnostics row below |
| Standalone Step 4 verification script (`diagnostics/verify_capture_*.js/html`) — **run 2026-08-22, all three assumptions confirmed** | Measured on both displays rather than trusted: (1) `bounds.width/height * scaleFactor` matched the actual captured `video.videoWidth/videoHeight` exactly on both — built-in (bounds 1496x967, scaleFactor 2 → expected/actual 2992x1934) and external QHD (bounds 2560x1440, scaleFactor 1 → expected/actual 2560x1440); (2) logged `getUserMedia` constraints carried only `chromeMediaSourceId`, no width/height, confirming native resolution comes back unshaped; (3) canvas dims matched video dims by construction (sized only from the video element, never from `display.bounds`); (4) `desktopCapturer` source→display mapping via `source.display_id` was unambiguous both times (`screen:1:0`→display 1, `screen:3:0`→display 3), not list-order-dependent. Script auto-runs and self-quits (modified 2026-08-22 for non-interactive launch) so it can be run from a background process, not just clicked by hand | Verified — Option 2's design is confirmed safe to implement in the real `captureRegion(bbox)` |
| **`track.getSettings()` is unreliable for actual capture dimensions immediately after track creation — read `video.videoWidth/videoHeight` instead** | Discovered by the verification script's own cross-check (built specifically to catch "two independently-sourced sizes disagreeing," per the Step 3 coordinate-mismatch bug shape). On the external QHD capture, `track.getSettings()` returned `{width:2992, height:1934}` — the *previous* (built-in display's) dimensions, not this stream's — while `video.videoWidth/videoHeight` on the same stream correctly read 2560x1440. Had the real crop math trusted `getSettings()`, it would have silently cropped the external monitor's screenshot using the laptop's resolution. Root cause not investigated further (plausibly a Chromium caching/timing quirk on the settings object right after `getUserMedia` resolves) — not needed, since the fix is simply which value to read, not why the other one lags. **`captureRegion()` must read dimensions from the `<video>` element's `videoWidth`/`videoHeight` (after `loadedmetadata` + a short frame-ready wait), never from `track.getSettings()`** | Active — load-bearing, implemented in `overlay.js`'s capture-request handler |
| **`getSettings()` staleness confirmed to be a real hazard, not a test-harness artifact** | Checked before writing the real rule: the diagnostic script called `getSettings()` on a freshly-created track from a freshly-created stream, *before* the shared `<video>` element was ever touched for that iteration — so the staleness wasn't caused by object reuse in the test script, it happened at the `MediaStreamTrack` API layer itself, right after a second `getUserMedia()` call in the same renderer session. That shape recurs in the real app: `main.js` creates the overlay's renderer once (`createOverlayWindow()`) and reuses it across every gesture via `show()`/`hide()`, so selecting on display A then later on display B *within one app run* reproduces the exact repro. Mitigation applies unconditionally in the real code regardless of cause | Active |
| **Display identity threaded from activation to finalize, not re-derived** | `captureRegion(bbox, displayId)` needs to know which display's `scaleFactor`/source to use, but the bbox itself (display-local logical coords) doesn't carry that. Two options: re-derive at finalize (e.g. from cursor position again) or store the identity `screen.getDisplayNearestPoint()` already computed at activation and thread it through. Re-deriving would be a second independent computation of the same fact — the exact bug shape burned twice already this session (Step 3's coordinate mismatch, the `getSettings()` finding above) — and could disagree with activation's answer if the cursor moved off that display mid-drag. Chosen: `activeDisplayId` set once at activation (`main.js`), read at finalize; the display's *geometry* (bounds/scaleFactor) is still re-fetched fresh at crop time via `screen.getAllDisplays()`, never cached, since this session also measured geometry changing across app restarts | Active — implemented |
| **Overlay content must not be visible during capture — fixed with a deterministic clear, not a settle timer** | **No trail-contamination bug was ever observed** — this was a theorized risk (found while grounding the implementation in the real `selection-finalized` handler: nothing enforced that the overlay's drawn trail/badge was gone before a screenshot), not a measured one; no real gesture had been run yet. **First attempt, shipped without being proposed first, then rejected on review**: hide the overlay, then wait `CAPTURE_SETTLE_MS` (100ms, no justification) before capturing. Called out on review as repeating the exact pattern this project already distrusts for `UNREGISTER_SETTLE_MS` — an unverified magic number in a hotkey-critical path, explained after the diff instead of proposed before it. Recorded here rather than silently replaced, per this file's own rule. **Fix actually adopted**: the overlay renderer clears every piece of visible content synchronously on keyup, before `finalizeSelection` is even sent — `trailCtx.clearRect()`, `badge.style.opacity = '0'` with its CSS `transition` force-disabled first (checked `overlay.html` directly: `#badge` has `transition: opacity 0.15s ease`, so setting opacity alone would fade over 150ms, not hide instantly), and `resetFlashEl.classList.remove('flash')` (no `transition` on that element, only a class-triggered `animation`, which cancels immediately on class removal with no disable-first trick needed — confirmed by reading the CSS, not assumed to match the badge's case). This is content-based, not time-based: nothing to mistune for a slower machine. `overlayWindow.hide()` is kept, demoted to hygiene only, no longer load-bearing for correctness. One side effect handled: forcing `badge.style.transition = 'none'` is an inline style and persists, so `resetState()` (run at every new activation) now explicitly restores it (`= ''`), or every subsequent gesture's badge would silently lose its intended fade-on-movement | Active — implemented and **verified on both displays, 2026-08-22** (see the row below); **self-reviewed 2026-08-22**, see the three rows below |
| **Step 4 self-review findings (2026-08-22)** | Three findings before commit, same rigor as Steps 1-3's review. (1) **Fixed**: `cropRect` rounded `x`/`width` (and `y`/`height`) independently — exact for the two scaleFactors measured (2 and 1, both integers, no rounding ever occurs) but could drift the right/bottom edge by up to 1px for a hypothetical fractional scaleFactor. Now rounds the two edges (`x`, `x+width`) and takes the difference — identical output for the cases tested, removes the risk for any future scaleFactor, at zero cost. (2) **Fixed, approved before implementing, then corrected again after a precise trace disproved the first version's own claim**: `capture.js`'s `CAPTURE_TIMEOUT_MS` only bounded the renderer round-trip, not the permission check or `desktopCapturer.getSources()` before it — a hang there would leave the hotkey unregistered indefinitely, the same failure class `STUCK_GESTURE_TIMEOUT_MS` exists to prevent, just for a different phase. Added `CAPTURE_OVERALL_TIMEOUT_MS` (main.js) wrapping the entire `captureRegion()` call via `Promise.race`, derived from `STUCK_GESTURE_TIMEOUT_MS + 2000` rather than a fresh unexplained number (checked first: `CAPTURE_TIMEOUT_MS=5000` itself had no written justification either — this reuses the one constant in the codebase that does). **First version's claim, traced and found incomplete on review**: since `Promise.race` doesn't cancel the losing side, the catch branch called `ipcMain.removeAllListeners('capture-result')`, stated as preventing a late reply from being mistaken for a later gesture's result. Precise trace showed this only holds in the sub-case where the stale reply arrives *before* a new gesture registers its own listener — `ipcMain.once()` has no concept of which request a reply belongs to, so if gesture B's own listener is already registered by the time gesture A's abandoned reply lands, B's listener would resolve with A's stale frame, crop it with B's bbox, and log success. Reachable only if `getSources()`/permission-check itself eats >2s (otherwise `CAPTURE_TIMEOUT_MS` fires first and cleans up on its own) — narrow, but not proven impossible, and the failure shape (silently wrong training data, no error) is the worst category for this project specifically. **Actual fix**: `requestId` tags each `capture-request`/`capture-result` round trip; `ipcMain.on()` (not `.once()` — a `.once()` listener would unregister itself on the *first* event regardless of whether the id matches, orphaning the real reply behind a stale one) checks the id and only resolves/removes itself on a match, ignoring anything else. Verified empirically (`diagnostics/verify_capture_requestid.js`), not just reasoned: with both listeners already registered, emitting the stale id first and the real one second, the current gesture's listener resolved with its own real data, untouched by the stale one — and the stale listener's own promise still resolved correctly on its own id, confirming it wasn't dropped either. `removeAllListeners` in main.js's catch block is kept but demoted to secondary hygiene (eager cleanup of a listener that would otherwise self-remove later) — no longer load-bearing for correctness now that `requestId` closes the gap regardless of timing. (3) **Checked empirically, no fix needed**: reasoned that `resetFlashEl`'s inline `opacity='0'` (set in the keyup cleanup) wouldn't need restoring in `resetState()` the way the badge's `transition` override did, since CSS animations take cascade priority over a plain inline style while running. Verified with a standalone test (`diagnostics/verify_resetflash_*`) rather than left as assumed: re-triggering `.flash` reads `opacity=0.9` immediately (the keyframe's `0%` value) despite the stale inline `0`, then decays normally (`0.857`→`0.789` over 30ms) — confirms the reasoning, no code change needed | Active |
| **Step 4 crop correctness — verified on both displays, 2026-08-22** | Verified by actually opening `crop.png`/`full.png` pairs and cross-checking: converting each logged bbox to physical pixels (× `scaleFactor`) and confirming that computed region of `full.png` matches `crop.png`'s content — not just "looks plausible." **Built-in display (scaleFactor 2)**: three gestures; one landed on live-chat disclaimer text and matched exactly, including the specific mid-word truncation. **External QHD display (scaleFactor 1)**: one gesture; landed on real terminal log text and matched **exactly across six lines, character-for-character, including left-edge word truncation** — the strongest confirmation of the pass, on the display that had zero coverage until this gesture. No trail/badge/reset-flash contamination in any image across either display — the deterministic-clear fix (see the row above) held up in practice, not just in reasoning. Distinct live-viewer counts across the built-in display's three `full.png` files also confirm each capture is genuinely fresh, not cached. **This closes Step 4's highest-risk item** — crop correctness across two displays with different scaleFactors — on both displays | Verified |
| **Screen Capture implemented** (`capture.js`, plus additions to `preload.js`/`overlay.js`/`main.js`) | `captureRegion(bbox, displayId) -> {cropPath, fullPath}` per system_design_plan.md §3.3, interface widened from the doc's bare `captureRegion(bbox)` to also take `displayId` (see the display-identity row above for why). Checks `systemPreferences.getMediaAccessStatus('screen')` first and throws a clear error if not granted — deliberately does NOT build the full pre-prompt explanation-screen UX from §7 yet; that's tracked separately in todo.md, deferred not dropped. All geometry math (source↔display matching, scaleFactor conversion, the actual crop via `nativeImage.crop()`) lives in `capture.js`; the overlay's renderer only captures a raw frame and hands it back, no math on that side. One captured frame per `captureRegion()` call — `crop.png`/`full.png` both derive from the same in-memory `nativeImage` (one crop, one uncropped), not two separate `desktopCapturer`/`getUserMedia` calls, so the pair can't drift against each other. Smoke-tested (`npm start` launches cleanly, hotkey registers, no errors) but **crop correctness on real gestures on either display has not been verified** — that requires a hands-on drag, which needs the user | Active — needs hands-on verification, see todo.md |
| **Chat Panel window: created fresh per session, closed (not hidden/reused) on panel-close** | Presented as a real fork before implementing (per CLAUDE.md's mentor-mode rule), approved by the user 2026-08-22. Alternative was reusing one persistent window like the Selection Overlay (`show()`/`hide()` per session). Chosen because: (1) "history starts empty" is then free — a fresh page load — instead of something that must be manually reset every reopen; (2) the window's own `closed` event is a single, unambiguous trigger for the temp-file move-or-delete invariant, with no separate "did I reset state for this new session" bookkeeping; (3) this codebase has already hit the "state silently surviving across operations that should be independent" bug shape twice (`track.getSettings()` staleness across captures, the capture `requestId` stale-reply race) — a fresh window per session removes that risk by construction rather than relying on correct manual reset every time. The overlay reuses its window because it's a latency-sensitive hot path (many gestures per run); the Chat Panel opens once per capture and isn't, so the tradeoff doesn't carry over. `setVisibleOnAllWorkspaces(true, {visibleOnFullScreen:true})` is set once per creation (in `createChatPanelWindow()`), which — because the window is never reused — already covers every session, same guarantee the overlay gets from setting it once at startup | Active — implemented 2026-08-22 |
| **Concurrent-session guard, first version: unregister the hotkey for the entire Chat Panel session** | Surfaced during Step 5 implementation, not previously discussed in any doc, and — this is the correction that matters — **implemented and reported to the user after the fact instead of proposed first**, violating CLAUDE.md's own mentor-mode rule. The problem being solved was real: `selection-finalized` used to unconditionally call `registerHotkey()` at its end, which — combined with the fresh-window decision above — would let a second gesture start (and spawn a second Chat Panel) while the first session's panel was still open, and two listeners on the same `chat-turn-added` IPC channel would then both receive every turn-added event regardless of sender, corrupting both sessions' turn counts. But the fix chosen (unregister on activation, re-register in `endChatSession()`) had two costs that weren't surfaced before building it: (1) it made a `Control+1` press while blocked a **structurally silent no-op** — the accelerator isn't bound to the app at all in that state, so no callback fires and there is no hook to give feedback from, which is exactly the failure shape (`"silently"`) this file's own multi-monitor row already flagged as the real cost in an earlier bug; (2) it extended the unregistered window from a few seconds (gesture + capture) to an entire, deliberately uncapped chat session, with no watchdog analogous to `STUCK_GESTURE_TIMEOUT_MS` — if `endChatSession()` ever failed to run, the hotkey would be stranded dead until app restart | **Superseded 2026-08-22, same day, before any hands-on testing** — see the row below |
| **Concurrent-session guard, corrected: keep the hotkey registered, guard inside the callback** | Proposed properly this time (options laid out, tradeoffs compared, user confirmed) before implementing. `registerHotkey()`'s callback now checks `chatPanelWindow` first: if a session is already open, it calls `.focus()` on the existing panel (reusing the same "focus the existing thing" response already built for the single-instance guard's `second-instance` handler, rather than inventing a new one) and returns *before* touching `globalShortcut.unregisterAll()` or any overlay-activation logic. `selection-finalized` reverted to unconditionally calling `registerHotkey()` after capture, same as before Step 5 touched it; `endChatSession()` no longer touches hotkey registration at all. This resolves both costs above: the accelerator stays registered throughout a session so a press now visibly does something (the window snaps to front and takes focus — a real, perceptible state change, plus a log line) instead of nothing; and the unregistered window returns to being scoped to gesture+capture only, same duration it's always had, so it doesn't add new exposure to the still-open todo.md item questioning whether `unregister()`-on-activate is even load-bearing — **that connection is exactly why the shorter-duration option matters, not just the feedback**. Rejected alternative, raised in review before picking this one: scoping the `chat-turn-added` IPC channel per-window and allowing genuinely concurrent sessions — nothing in system_design_plan.md or decisions.md ever asked for concurrent sessions, and the product's own mental model ("hold hotkey, circle, chat, close") reads as inherently one-at-a-time, so this would have been solving a problem nobody has. **Hands-on verified 2026-08-23**: pressed `Control+1` three times in a row while a session was open — log shows all three correctly hit the guard (`Hotkey pressed while a Chat Panel session is open — focusing it instead of starting a new gesture`), none started a second overlay/panel, and the session's own close still ran normally afterward | Active — implemented and verified 2026-08-22/23 |
| **Hold-to-talk vs. the accelerator — investigated and resolved 2026-08-23** | Raised on review before any hold-to-talk code was written: does `Control+1`, left registered under Option B, still claim `Digit1` even once the Chat Panel already has real OS focus? Verified empirically with a temporary diagnostic (`chatPanelPreload.js` + a logging script in `chatPanel.html`, since replaced by the real implementation) rather than trusting the old row 27 claim above. **Result, from real presses, not inference**: 3 of 4 test presses showed `Digit1` never reaching the renderer at all — not delayed, not eventually via auto-repeat, not even across one genuine 4.2s hold — while `ControlLeft`'s keydown/keyup arrived normally every time. The 4th press (a clean, immediate, fully-normal `Digit1` sequence, no accelerator firing at all) is best explained by the already-established chord-timing rule (decisions.md: modifier must go down *before* the trigger key or the global match fails outright) — the renderer timestamps show `Digit1`'s keydown arriving *before* `ControlLeft`'s in that one case, the reverse of the required order. Since modifier-first is also the *only* order that makes the base hotkey register at all, this isn't an occasional edge case — every user hits the claiming behavior on every hold-to-talk attempt they'd actually make correctly. This settles the mechanism as the same one Step 3 already found (chord-timing / accelerator claiming the trigger key), not a new focus-state-dependent behavior, and it's a **harder** version of the overlay's original problem: the overlay's guard unregisters immediately so auto-repeat rescues it after ~445ms; the Chat Panel's guard (previous row) never unregistered at all, so without a fix `Digit1` would never reach the renderer for any hold, of any length. Considered and rejected: building on `ControlLeft` reaching the renderer reliably as a substitute end-signal — tempting given the data, but rejected against the established general principle (track the non-modifier trigger key, never the modifiers) rather than inverting it off one scenario's evidence. **Fix**: `registerHotkey()`'s `chatPanelWindow` guard now branches on `chatPanelWindow.isFocused()`. Focused → treat exactly like a region-selection gesture: `globalShortcut.unregisterAll()`, send `'hold-to-talk-start'` (renderer bootstraps its `holding` flag from this signal, never a local `keydown` — same principle as the overlay, decisions.md), arm a hold-to-talk liveness watchdog (kept as separate state from the gesture one — `holdToTalkWatchdog`/`seenHoldToTalkKey` — since the two can never actually overlap but end differently on timeout, and reuses `STUCK_GESTURE_TIMEOUT_MS`/`INITIAL_KEY_DEADLINE_MS` unchanged rather than inventing new constants, since the same two-stage reasoning applies unchanged), and end strictly on the renderer's real `Digit1` `keyup` (`'hold-to-talk-end'`) — never a timeout; the watchdog's `'hold-to-talk-cancel'` path is a distinct signal for the stuck case only, never called on a normal end. Not-focused case unchanged from the previous row (just focus the panel). Also added: if the Chat Panel window closes while a hold is active, its `closed` handler clears the watchdog and re-registers immediately rather than waiting out up to 5s, since no more events for a now-gone window are ever coming. **Hands-on verified 2026-08-23**: 3 real hold-to-talk cycles in one session (~3.1s, ~8s, ~2.2s) — every one showed `starting hold-to-talk` → the renderer's `started` log → **no watchdog force-close** (notably the 8s hold, well past the 5s silence threshold, which only survives if the auto-repeat liveness pings are genuinely re-arming the watchdog throughout, not passing by luck) → `ended by real keyup` → clean re-registration, confirmed by a normal capture gesture immediately after. Real audio capture (whisper-node-addon) is explicitly not built yet — this closes only the key-mechanics loop that was blocking it, marked with `TODO(next slice)` at the two spots (start/end) where recording plugs in | Active — implemented and verified 2026-08-23 |
| **Codebase reorg**: `main.js` slimmed to orchestration only; `src/overlay/`, `src/chatPanel/`, `src/shared/` introduced, grouping files by UI surface / shared backend rather than one flat directory. Dead Step 1 placeholder (`index.html`, `createWindow()`/`win`) deleted in the same pass, not carried forward or rehabilitated into the new Main App Window | Two distinct UI surfaces (ephemeral Overlay+Chat Panel flow vs. the new persistent Main App Window, see below) now share backend modules, and `main.js` was already at 880 lines before any Main Window code was added — real god-file risk, not hypothetical. `src/mainWindow/` and `src/shared/settings.js` deliberately **not** created yet — nothing lives in them until Phase 2.2 actually starts; empty stand-ins would be speculative scaffolding (Simplicity First). The placeholder window's own todo.md cleanup condition ("once Electron reaches equivalent functionality") is satisfied by the Main Window being built in this same pass, so deleting it now is that trigger firing, not an exception to "don't delete unasked." Chat Panel already set the precedent for building fresh rather than reusing this same placeholder — the Main Window follows the same reasoning. Move-plan, path fixes, and dead-code removal independently verified against the actual diff; real hold-drag-release-type-close gesture run post-reorg confirmed no regression (log sequence + on-disk file/JSONL check) | Active — implemented 2026-08-24, commit `8d787cf` |
| **Open, flagged during the reorg, deliberately not decided**: once the Main App Window is persistent, does closing it quit the whole app (today's `window-all-closed` handler no-ops on darwin already, so the app technically already doesn't quit when all windows close) or should it specifically *hide* rather than destroy, the way a menu-bar/dock-icon utility usually keeps running with the hotkey still live? This wasn't a real question before — nothing genuinely persistent existed for a user to "close" | **Superseded 2026-08-24 — resolved, see the row below** |
| **Resolved: Main App Window follows normal macOS dock-app convention** — closing its own close button does NOT quit the app; `Cmd+Q` quits fully | `window-all-closed`'s existing darwin no-op already handles the non-quit-on-close case correctly — no code change needed there, it was already right by accident of following the platform default. `Cmd+Q` already works too, via Electron's default app menu: this repo has never called `Menu.setApplicationMenu()`, so the default `Cmd+Q`→`app.quit()` binding is already active — no new code needed for that either. **One real gap this decision does require, scoped to Phase 2.2, not built now**: an `app.on('activate', ...)` handler to recreate/show the Main Window when the dock icon is clicked with no windows open. Standard macOS lifecycle hook; doesn't exist in this codebase yet since nothing persistent has existed until now | Open question closed; implementation note tracked in todo.md for Phase 2.2, not yet built |
| **New fine-tuning goal, named as tension rather than silently blended in: also train for response quality** (helpfulness, concision, tone), not just visual-reading accuracy | Directly in tension with the existing Phase 5 eval stance (row above: "not conversational polish"; implementation_plan.md Phase 5: "not conversational flair... a terse-but-correct answer is a win"). Not fully reconciled here — "concise and helpful" and "not flowery/polished" aren't strictly the same axis, but they sit close enough that Phase 5's scoring criteria will need a real look once response-quality-labeled data actually exists, not an assumption the two goals coexist cleanly without friction | Open — new goal recorded, tension flagged, not reconciled with Phase 5's scoring criteria |
| Response-quality data collection: resolved on the **lighter-weight approach** — broaden the existing Phase 3 hand-review pass (edit for quality/concision/tone too, not just correctness, including on 👍'd conversations) plus one small Phase 2.1 addition (an optional free-text note captured at 👎-time) | Two heavier alternatives explicitly considered and deferred, not forgotten: per-turn feedback (rejected — conversation-level feedback is already the resolved granularity, see the conversation-model row above) and a dedicated preference-pair UI (rejected — real scope for a need not yet confirmed; revisit if the lighter approach proves insufficient once real data exists) | Active — implemented 2026-08-24, see `data_pipeline.md`/`skills/dataset-pipeline/SKILL.md` for the broadened review workflow |
| **Schema change: `feedback_note: string \| null`** added to the per-conversation record (system_design_plan.md §5, `data_pipeline.md`, `dataset-pipeline` skill, `localLogger.js`) | Small and additive — existing records unaffected (field simply absent on records logged before this change; `logConversation()` defaults it to `null`). Captures an optional free-text note when 👎 is clicked ("what was wrong?"), never required to submit; only ever set alongside `feedback: 'thumbs_down'` — switching back to 👍 clears it client-side before it's ever sent. **Hands-on verified 2026-08-25**: 4 real sessions covering all 3 required scenarios — 👍/no-note (×2), 👎/with-note (`feedback_note` matched the typed text exactly), 👎/no-note — confirmed in `conversations.jsonl` and against the permanent crop files on disk, not just the log | Active — implemented and hands-on verified 2026-08-24/25, Phase 2.1 |
| **Note submission changed from live-streaming to explicit (Enter), 2026-08-25** | The first Phase 2.1 pass sent the note field's value to main on every keystroke, so "submitting" the note was invisible — it just happened as a side effect of typing, with no confirmation and no way to tell the note had been captured. Raised on review: is that the actual gap, or does the feature already exist as-is? User confirmed the gap was the missing explicit affordance, not the underlying capture mechanism. **Real behavior change, not just UI polish**: an unsubmitted note is now discarded on close rather than auto-captured — `chatPanel.html`'s `confirmedNote` only updates via `submitNote()` (Enter), never via the field's live value, so typing without confirming genuinely loses the note | Active — implemented 2026-08-25, pending hands-on verification — see todo.md |
| **Dedicated send button (claude.ai-style, tried first for the row above) — built, then dropped same day** | User reported the button "does nothing" after real testing; a real session's `feedback_note` was nonetheless captured correctly ('hi'), meaning it went through via Enter, not the click. Code inspection found no wiring bug (IDs, listeners, and the `disabled`↔text-length binding all checked correctly) — most likely explanation, not fully confirmed, is the button was clicked while still in its `disabled` state (native `<button disabled>` swallows clicks before any JS runs), which would look exactly like "does nothing." Rather than debug further, raised the actual question directly: is the button even needed, since Enter alone already delivers the real behavior (visible ✓ confirmation, discard-if-unsubmitted) that motivated building an explicit-submit affordance at all. User agreed to drop it. Replaced with a plain "✓ Noted" text flash on Enter — same transient-cue convention as `flashRecordingStatus()` elsewhere in this file, no button/disabled-state machinery to maintain | Dropped 2026-08-25 — Enter-only is the shipped design, see the row above |
| Personalization vs. authentication, explicitly separated | A local display-name field in Settings is cosmetic personalization only — not a login, not an identity check, creates no account or session. Real user authentication remains exactly as already scoped at Phase 7 (Supabase Auth), unchanged by this | Open — proposed, not yet built |
| Main App Window judged justified now, not speculative | Accelerator configurability (already an open todo.md item) and a way to browse `conversations.jsonl` (which already exists with no viewer) were already-flagged real needs, not invented scope for this session | Open — proposed, not yet built |
| Single persistent `BrowserWindow` (`mainWindow.html`) with client-side view switching (sidebar: Captures/Settings/Help), over three separate `BrowserWindow`s | Matches the actual mental model (one settings/captures app, not three independent windows) and avoids tripling the window-lifecycle bookkeeping (focus, secondary-display Space handling, close semantics) this project has already spent real debugging effort on for the Overlay and Chat Panel. Opened via dock icon/menu, not the hotkey — a separate lifecycle and activation path from Overlay/Chat Panel entirely | Open — proposed, not yet built |
| Settings' hotkey-change flow requires a real hold-drag-release gesture test before saving, not a static validity check | Directly reuses this project's own "registration success proves nothing" lesson (row above) — a candidate accelerator can register cleanly and still fail for reasons no static check catches. Failure/timeout handling during that test is explicitly not yet decided | Open — proposed, not yet built; failure/timeout behavior flagged, not designed |
| Theme via Electron's `nativeTheme` API + CSS `prefers-color-scheme`, light/dark/system | Both are already built into Electron/Chromium — no new dependency needed for a three-state theme toggle | Open — proposed, not yet built |
| Dictation-language setting reopens the already-flagged whisper.cpp model-size tradeoff (todo.md: base.en vs. multilingual/small.en), rather than being a new independent decision | Swapping off `base.en` to a multilingual model has its own accuracy/size/speed tradeoff distinct from language coverage — needs its own verification pass per language actually offered, not assumed to work correctly just because the architecture supports swapping models | Open — folds into the existing todo.md item, not yet decided |
| App-wide UI language / full i18n deferred, shown disabled ("coming soon") in Settings rather than omitted or silently built | Translation debt scales with a UI surface that's still actively changing (this session alone added three new pages) — building real i18n now means re-translating through every future UI change. Shown-but-disabled communicates intent without taking on that cost yet | Open — proposed, not yet built |
| Settings' Data and privacy section (capture storage location + open-folder action + clear-all action) justified by the pre-existing privacy-first constraint (system_design_plan.md §1), not new scope | §1 already commits to "no silent capture, no unnecessary retention" — a page letting the user see/find/clear their own captured data makes that commitment visible and actionable, not a new promise | Open — proposed, not yet built |
| **Phase 3 curation signal: a user rephrasing/correcting their question right after an answer treated as a real implicit negative signal**, even on conversations with no 👎ever clicked | A user immediately rephrasing or correcting ("no I meant...", "that's not right") is a real behavioral signal, more reliable than self-report since it doesn't depend on the user remembering to click anything — worth flagging for priority review during Phase 3's hand-review pass regardless of explicit `feedback`/`feedback_note` | Open — proposed, not yet built; nothing to implement until Phase 3 starts |
| **Phase 3 curation signal: a short conversation with no follow-up is only a weak, ambiguous tiebreaker signal, never ground truth alone** | Quick-and-correct is indistinguishable from user-gave-up-and-left purely from turn count — usable at most to break a tie between otherwise-similar review candidates, never as a standalone quality signal | Open — proposed, not yet built |
| **Automated-judge curation pre-filter — considered and explicitly deferred, not dropped silently** | Extending Phase 5's planned Mistral-as-judge to also triage Phase 3 curation volume was considered and rejected for now, on two grounds: (1) no real data volume yet exists to justify automating triage, and (2) using an unvalidated judge to shape which training data gets selected before Phase 5 ever confirms that judge agrees with human grading has the dependency order backwards — Phase 5's whole purpose is validating the judge against manual grades, so leaning on it earlier for a different purpose skips that validation | Deferred — explicit revisit trigger: Phase 5 validates the judge against manual grades **AND** curation volume genuinely becomes a bottleneck a human can't handle alone |
| **This Electron install/environment doesn't auto-promote to a Dock-visible app — needs an explicit `app.dock.show()`** | Discovered during Phase 2.2's first real verification attempt: no Dock icon appeared at all, from three independently-tested launch methods (this session's background/`nohup` launch, the sandbox explicitly disabled, and — the one that actually mattered — the user's own interactive Terminal). All three showed the identical macOS-confirmed state (`osascript`'s `System Events ... background only of process "Electron"` → `true`), ruling out launch context/sandboxing as the cause before touching any code. Root-caused with a throwaway diagnostic script (not the real app) that called `app.dock.show()` explicitly and re-checked the same `osascript` query — flipped to `false` immediately, confirming the fix rather than assuming it from Electron's docs. **Why nothing caught this before**: every prior "real gesture" verification in this project relied on the global hotkey + `app.focus({steal:true})`, both of which work regardless of Dock-icon/activation-policy state — Phase 2.2 is the first feature that actually depends on the Dock icon existing (for `app.on('activate', ...)` to ever fire). Fix: one line, `if (app.dock) app.dock.show();`, inside `app.whenReady().then()` in `main.js`, guarded on `app.dock` existing since it's a macOS-only API. **One unreproduced anomaly along the way**: after the fix, one real test session saw the Dock icon disappear again mid-session (confirmed independently via the same `osascript` query at the time — a real observation, not a misreport). Investigated by adding temporary `[dock-diag]` logging (`app.dock.isVisible()`) at every real lifecycle checkpoint — window creation, reuse, close-intercept before/after `hide()`, and both `app.focus({steal:true})` call sites (overlay + Chat Panel) — then re-running an equivalent-or-heavier interaction sequence. The fully-instrumented rerun showed `dock visible: true` at every single checkpoint with zero anomaly, including through 4 separate dock-icon activations, a close-intercept, and a real `Cmd+Q` that correctly bypassed the intercept and let the app actually quit. Left as a genuinely unreproduced single observation rather than a confirmed recurring bug — diagnostic logging removed once its purpose was served (kept out of normal log output, matching how prior temporary diagnostics in this project were handled). **Revisit trigger**: if the disappearance recurs, re-add the same `[dock-diag]` checkpoints rather than re-deriving them | Active — implemented and hands-on verified 2026-08-25, see todo.md for the full real-session evidence |

## Hold/release detection on macOS + Electron

*Consolidated 2026-08-21 from a full session of debugging. The table rows above
record individual decisions and their reversals; this is the mechanism they all
follow from. If you only read one thing before touching gesture input, read this.*

### The core problem

Electron's `globalShortcut` fires once per completed accelerator press and has no
release event. A hold gesture therefore needs two stages: the global accelerator
starts it, then the overlay window — which takes focus on activation — tracks the
same physical key with ordinary window-scoped `keydown`/`keyup`.

That handoff is where everything goes wrong, in four distinct ways. All four were
discovered separately, each initially misdiagnosed as the others.

### 1. The accelerator eats the first keydown — the root of most symptoms

`globalShortcut` consumes the trigger key's original `keydown` to fire the
accelerator. **The overlay never receives it.** The window stays completely blind
to the key until macOS's auto-repeat begins — measured at a consistent **~445ms**
on default settings — at which point repeat `keydown`s start arriving normally.

The consequence that matters: **a key the window never saw also produces no
`keyup`.** Release before auto-repeat starts and the gesture has no way to end.
It hangs until a watchdog kills it.

This single fact explains a cluster of symptoms that looked unrelated for most of
the session:
- "Quick taps intermittently lose the keyup" — not intermittent at all. It is
  deterministic: release under ~445ms, lose the gesture.
- "Sometimes pressing the hotkey does nothing" — the accelerator is unregistered
  for the entire duration of a hung gesture (see §2), so every press during that
  window is silently dead. Downstream of the same bug, not separate.
- "A tiny trail lingers for 5 seconds after a tap" — the trail is drawn correctly
  from real hand movement; what is broken is that nothing ends the gesture.

**Mitigation** (`INITIAL_KEY_DEADLINE_MS = 2500`): if no trigger-key event arrives
within 2.5s, conclude the key was released early and close immediately. Chosen over
a snappier ~1.2s because macOS's "Delay Until Repeat" can be set as slow as ~2s and
that setting exists for accessibility/motor reasons — a tighter deadline would
force-cancel genuine holds for exactly the users who need it. **Deferred
alternative**: read the real value (`defaults read -g InitialKeyRepeat`) and derive
the deadline. More precise, but shelling out plus unit conversion plus a fallback
path is real complexity for a Phase 1 prototype. Not built; revisit if 2.5s bites.

### 2. The accelerator must be unregistered during the gesture

While the accelerator stays registered, macOS's global-hotkey layer claims the
trigger key's `keyup` exclusively — it never dispatches to a window, even a focused
one. So the activation callback calls `globalShortcut.unregister()` and re-registers
on finalize.

**Caveat worth knowing**: the diagnostic that originally "confirmed" this was
collected on `Alt+Space`, and that asymmetry has a competing explanation (§3). The
unregister call is still in the code and gestures work with it, but whether it is
load-bearing is untested on a non-Option accelerator. Cheap to settle; nobody has.

### 3. Input-method layers eat the trigger key — Option is one case, not the only one

Option is macOS's special-character modifier — `Option+Space` emits U+00A0,
`Option+D` emits `∂`. Any `Option+<key>` chord is routed through the
text-input/composition layer, which consumes the trigger key's raw events entirely.
Measured: **25 consecutive gestures on `Alt+Space` delivered zero `Space` events of
either kind**, while `AltLeft`'s keyup arrived normally in the same gestures.

**`Control+Space` failed identically**, via a different input-method layer: macOS's
input-source switcher grabs the chord alongside us — visible in the log as an
`IMKCFRunLoopWakeUpReliable` error — and swallows Space's events. The accelerator
fires, the window is focused, and no key events ever arrive. So this is a **failure
class, not one bad chord**: any macOS input-method layer sitting between the OS and
the window can consume the trigger key. Check for it first when a new accelerator
misbehaves.

The trap: a reserved-global-shortcut check cannot detect any of this. `Alt+Space`
passed that check cleanly and was still completely broken. **Any candidate
accelerator must be empirically verified to deliver its trigger key's keyup**, not
just checked against a list. Two further silent failure modes found the same way:
bare F-keys are consumed by macOS hardware functions before Electron sees them
(`F6` toggled Do Not Disturb and never fired the callback), and `` Control+` ``
registered successfully but its callback never fired at all.

**Chord timing matters too**: macOS only matches a global accelerator when the
modifier is already down. Pressing both keys simultaneously frequently fails to
activate — which masqueraded as "this candidate doesn't work" for `Control+Space`
and as "this key needs a harder press" for `Escape`. Press the modifier first.

### 4. Focus instability — mostly a testing artifact, not a real defect

**This section originally claimed focus acquisition was unreliable and was the
largest remaining source of failed gestures. That was wrong, and the error is
instructive.** Those measurements were taken while *multiple Electron instances
were running simultaneously* — each `npm start` during debugging left the previous
app alive, and every instance registered the same accelerator and called
`app.focus({steal:true})` on its own overlay. The instances fought over focus.

Measured cleanly, one instance only (run `bfjomboj8`, verified sole instance in its
window): **`isFocused` `true` immediately on 32 of 33 gestures**, and the single
`false` case still finalized successfully. Under contamination (run `bwyutwf7p`) the
same measurement was `false` on **6 of 8**. Focus acquisition is fine.

**Direct confirmation, not inference** (checked 2026-08-21 when the claim was
challenged): a single physical keypress at `04:48:33` appears in **five separate
instance logs** within 2ms of itself —

```
b7s3z9kq2  04:48:33.598Z  Hotkey pressed [Shift+Escape]
bs4s3g6r3  04:48:33.597Z  Hotkey pressed [Shift+Escape]
bppia7rqz  04:48:33.599Z  Hotkey pressed [Shift+Escape]
bwyutwf7p  04:48:33.599Z  Hotkey pressed [Shift+Escape]
bve69xxpn  04:48:33.597Z  Hotkey pressed [Shift+Escape]
```

So macOS **does** dispatch one global accelerator to every registered process, each
of which then called `app.focus({steal:true})` on its own overlay. The same pattern
recurs at `02:38:05` (5 instances). That is the contamination mechanism, observed
directly. Note `globalShortcut.register()` returned `true` in every instance and no
duplicate-registration error was ever emitted — another instance of "registration
success proves nothing".

Two lasting lessons: **kill stray instances before measuring** (a global-shortcut
app is especially vulnerable, since duplicates silently compete for the same
accelerator and focus), and treat any "intermittent" finding as suspect until the
environment is known clean. A residual real effect may exist — one late-focus case
did appear — but it is rare and did not cost a gesture.

### Consequences for anything built on top of this

- **The trigger key's `keyup` is the only reliable end-of-gesture signal.** Track
  the non-modifier trigger key, never the modifiers.
- **Bootstrap "is the key held" from the activation signal**, never from a local
  `keydown` — per §1 the original keydown never arrives. Auto-repeat keydowns do
  arrive after ~445ms and are useful as a liveness signal, but they are not a
  substitute for the bootstrap.
- **Never draw from the activation point.** Two independent problems corrupt the
  first drawn segment, and both are fixed by consuming two events before drawing:
  discard the synthetic one, then anchor on the next.
  1. *The synthetic activation mousemove has bogus coordinates.* Showing the overlay
     under the cursor makes Chromium emit a mousemove 13-54ms after activation whose
     position is off by a location-dependent few pixels — deterministic and
     pixel-identical across repeated activations from a parked cursor. Left in, it
     drew a phantom trail on every activation and let taps finalize bogus selections
     (one measured at 6×31px).
  2. *The activation point itself goes stale when the cursor is moving.* It is
     `getCursorScreenPoint()` sampled at hotkey-press time. Press the hotkey while
     still moving the mouse and it is already wrong: measured 29px off from where the
     cursor was 29ms later, which drew a 29px line the instant real movement began.
     This is why the artifact appeared *only on the first gesture* — by the second the
     cursor is parked, so the activation point happens to be accurate.
  Consequence worth knowing: a selection now begins where drawing actually started,
  not where the cursor was at press time. That is the more correct behavior — the
  bbox previously included a point the user had already left.
- **`uiohook-napi` is still deferred, and its revisit trigger has not fired.** The
  trigger is *deliberate holds* losing keyups. Every failure traced back to one of
  the four causes above — accelerator choice, early release, or focus instability —
  never to `globalShortcut` being architecturally incapable. It looked like the
  trigger had fired twice this session and both times the real cause was elsewhere.
  **Rule out §1–§4 before concluding the mechanism is at fault.**

### How this was actually debugged — the method mattered more than the theories

The activation-trail bug took **four** wrong diagnoses before it broke: a residual
point from the previous gesture, a stale queued event, first-activation slowness,
and a coordinate-space mismatch between the two sources. Each was plausible, each
fit the symptom, and the first three were individually disproven by measurement.

What finally cracked it was not a better theory. It was logging the **anchor value
alongside each event** — `anchor=(858, 482)` next to `client=(862, 452)` — which made
the 29px staleness directly visible instead of inferred. Every earlier round had
instrumented the *events* while treating the anchor as ground truth. **The bug was
in the variable nobody was measuring.**

Generalisable lesson for this codebase: when several plausible theories keep half-
fitting, stop generating theories and start logging the values currently assumed
trustworthy. The same pattern showed up twice more this session — `isFocused` looked
like a real defect until a clean single-instance run showed the measurements were
contaminated, and accelerators looked broken until the user noticed press *order*
mattered.

### Things measured and confirmed correct (do not re-suspect)

- `resetTrackingPoints()` clears prior-gesture state correctly. Verified across 17
  instrumented gestures: every gesture's first mousemove referenced its own
  activation point, never the previous gesture's end. The "residual point from the
  last gesture" hypothesis was tested and **disproven**.
- The stale-queued-event hypothesis for the phantom trail was tested against event
  timestamps and **disproven** — those events land 13–54ms *after* activation, not
  before. Do not re-derive this; the fix that would have followed from it (drop
  events timestamped before activation) is a no-op.

| **Temp-file lifecycle, designed 2026-08-22 (no code yet)** | Step 4's `captureRegion()` writes `crop.png`/`full.png` to `app.getPath('temp')`, and system_design_plan.md's Step 8 interface (`logConversation(cropPath, contextPath, ...)`) takes those as literal paths — if nothing ever moves them out of temp before that path gets written into a permanent conversation record, the logged path can end up pointing at a file the OS is free to reap, silently corrupting the dataset with no visible failure at write time. **Core invariant**: at panel-close time, for every session, exactly one of two things happens — never neither, never both. (1) **Logged conversation**: Step 8's Local Logger *moves* (not copies) `crop.png`/`full.png` out of `app.getPath('temp')` into permanent app storage (e.g. `app.getPath('userData')/captures/`) *before* writing the path into the conversation record — closes the gap above by construction. (2) **Non-logged session** (cancelled, no real turns, whatever the eventual don't-log-empty/broken-conversation rule ends up being, per system_design_plan.md §7's "user closes mid-request" case) — explicit delete of that session's temp files at close time, not left for the OS to reap on its own schedule. **Startup sweep as a crash-safety net only, not the primary mechanism**: use a dedicated subfolder under temp (e.g. `temp/screen-region-captures/`, not the OS temp root directly) so a launch-time sweep can safely delete anything past an age threshold (a day or two is reasonable) — this only ever touches files that should already have been moved or deleted by (1)/(2) and weren't, i.e. an orphan from a crash/force-quit mid-session. **Explicitly decided against — no calendar-based auto-delete for logged, permanently stored images** (e.g. "delete after a week/month"), for two reasons worth keeping visible, not just the conclusion: (a) it risks the exact failure this whole decision exists to prevent — a dataset row silently pointing at a file that's been auto-deleted before Phase 3 curation ever runs, and curation happens on whatever schedule fits around the user's classes, not immediately after capture; (b) storage volume doesn't currently justify it — rough math at realistic solo-project usage (~20-30 captures/day) lands around 1.5-2GB/month, nowhere near a real problem on modern storage, so building deletion logic against a speculative problem violates Simplicity First. If storage ever becomes an actual concern later, the right fix is a size-based cap that prunes oldest *unlabeled* (no thumbs-up) images first and never touches labeled ones — not a blind timer; noted as a deferred future option in todo.md, not built now. This is groundwork for Step 5's close-handler design and Step 8's Local Logger — Step 4's own job (save to temp) was correct as built, this is not a Step 4 defect | **Implemented 2026-08-22** — `main.js`'s `endChatSession()` carries out exactly this branch (delete-and-discard when `turns.length === 0`, else move-then-[log later]), called from the Chat Panel window's `closed` event. `moveFile()`'s rename-with-EXDEV-fallback verified against real files, including the fallback path itself (forced via monkey-patched `renameSync`), in `diagnostics/verify_move_file.js`. `capture.js` now writes into the `temp/screen-region-captures/` subfolder this design assumed (previously wrote straight to the OS temp root — a gap between this design and the already-built Step 4 code, closed here). **Not yet implemented**: the actual `logConversation()` write (Step 8) — the "keep" branch currently only logs to console and leaves a `TODO(Step 8)` at the exact call site. **Hands-on verified 2026-08-23**: 4 real gestures across both the built-in and an external (Dell) monitor — panel opened and took real keyboard focus every time; each close logged "discarding temp captures" (0 turns, as expected until turns are wired) and the temp `screen-region-captures/` folder was confirmed empty on disk afterward (checked directly, not inferred from the log); permanent `captures/` folder correctly never got created, since the keep-branch has never had a reason to fire yet |

| **Step 6 scope: core multi-turn Mistral call only, hybrid full-screenshot context deferred** | `todo.md`'s Step 6 implementation notes listed the downscaled-thumbnail-every-call + `requestFullScreenshot()` tool-calling escalation + `prompt_cache_key` alongside the base port. Raised as an explicit scope question before implementing (per CLAUDE.md's mentor-mode rule) rather than folding all of it in silently. User approved scoping down: build and verify just the crop-image + multi-turn-history call now, matching Step 6's own stated verify criteria (single-turn answer, coherent follow-up); track the thumbnail/tool-escalation/prompt-caching feature set as its own explicit todo item, done deliberately before Step 7's wiring rather than debugged simultaneously with the first real external-API integration in this codebase | Active — see todo.md for the tracked follow-up item |
| **`visionClient.js`'s `askAboutRegion(imagePath, conversationHistory)` implemented** (system_design_plan.md §3.5) | JS port of `mistral_vision_query.py`, extended for multi-turn per decisions.md's existing "ported to JS" row. **Contract on `conversationHistory` stated explicitly, per review**: every turn is always a plain `{role, content: string}` — the caller (eventually the Chat Panel/main.js at Step 7) never hands in an already-image-wrapped turn 0. The image-parts wrapping is `visionClient.js`'s own private concern, reapplied to turn 0 fresh on every call rather than tracked as one-time state — since Mistral's chat API is stateless and the full history array is rebuilt from scratch on every call anyway, wrapping turn 0 unconditionally is sufficient to keep the image "in view" for every follow-up with no separate "have I sent it yet" bookkeeping. Failures are thrown as `Error`s carrying a `.code` (`'network'` \| `'rate_limit'` \| `'api_error'` \| `'malformed'`) rather than formatted UI text — this module doesn't know about the Chat Panel at all; routing a `.code` to one of §7's user-facing messages is the Response Handler's job at Step 7, matching the §3.5/§3.6 component split. A `REQUEST_TIMEOUT_MS = 30_000` `AbortController` timeout was added (not in the Python reference) so an unreachable API surfaces as a `'network'` failure within the session rather than hanging indefinitely — justified in-code per review, no bare unexplained constant. **Verified 2026-08-23** via `diagnostics/verify_visionclient.js` against a real crop on disk and the real Mistral API: single-turn call returned an accurate description of the actual image content (a code screenshot, correctly described down to the specific logic); a follow-up call against the growing history returned a coherent, consistent condensed restatement — not a fresh unrelated answer, confirming history actually round-trips; a missing-API-key case threw the expected clear error. Endpoint used: `POST https://api.mistral.ai/v1/chat/completions` (OpenAI-style REST shape) — not pinned anywhere else in this repo, noted as read from general knowledge of Mistral's API rather than a doc in this codebase | Active — implemented and verified 2026-08-23, not yet wired into the live Chat Panel (that's Step 7) |
| **API key loading: `.env` file + `dotenv`, not a shell export** | User's explicit, standing instruction: API keys are set directly by them into a local file, never relayed through chat or a shell command either party runs — this applies going forward generally, not just to this key. `dotenv` added as a dependency; `require('dotenv').config()` runs as a side effect of requiring `visionClient.js`, so `process.env.MISTRAL_API_KEY` is populated automatically for anything that requires that module (the diagnostic now, `main.js` once Step 7 wires it in) with no separate loading step needed anywhere. Node's built-in `--env-file` flag (available on this project's Node 23) was considered and rejected — it's a flag to the `node`/`electron` binary invocation, not reliably equivalent when the process is launched via `electron .` for the main process, and it wouldn't help the standalone `node diagnostics/*.js` scripts uniformly the way requiring `dotenv` does. `.env` confirmed added to `.gitignore` and confirmed never committed (`git log --all -- .env` empty) before this was relied on | Active |

| **Step 7 implemented: Response Handler (`responseHandler.js`) + `main.js` wiring + Chat Panel loading/error/retry UI** | `handleUserTurn(chatPanelWindow, cropPath, turns)` orchestrates one `askAboutRegion()` call per submitted turn: sends `'chat-loading-start'` immediately (§6), then either appends the assistant reply and sends `'chat-assistant-reply'`, or maps the failure's `.code` to one of §7's messages and sends `'chat-error'` — this module formats no UI text of its own beyond that lookup table, keeping the §3.5/§3.6 component split real. Returns `{controller, done}`: `main.js` tracks `controller` for cancel-on-close and awaits `done` (which never rejects) to clear its "call in flight" guard — without that second part, a first call completing would permanently block every later turn, a real bug caught and fixed before this ever ran live. `visionClient.js`'s `askAboutRegion()` gained an optional `{signal}` param, chained into the same `AbortController` that already drove the 30s timeout; the catch block distinguishes a deliberate external abort (`.code = 'cancelled'`) from a real network failure by checking whether the *external* signal specifically fired. `chatPanel.html` gained a loading indicator, disables `#input` while a reply is pending (also the guard against overlapping submissions — `main.js` has a matching defensive check), and renders failures as an inline `.turn.error` bubble with a **Retry** link that resends the same still-unanswered turn (`main.js`'s `onRetry()` guards on the last turn actually still being `role:'user'`) | Active — implemented 2026-08-24, see the two rows below for its full verification (initially reported prematurely as done — corrected on review, see below) |
| **Step 7 initially reported "complete" without exercising cancel-on-close, error routing, or retry — corrected on review before accepting the claim** | First verification pass only exercised the happy path (real multi-turn sessions, real replies, loading indicator confirmed visually) and got sidetracked into the Incident B regression chase (row below) before any failure-mode testing happened. When asked to close Step 7 out, review caught that "Step 7 complete" was being reported with zero mention of cancel-on-close or `.code`-based error routing — both built, neither tested. Both were then genuinely exercised (see the row below) rather than left as reasoned-through-but-unverified, per this project's own standing bar for what "verified" means | Corrected 2026-08-24 — see the verification row below for what was actually run |
| **Step 7 cancel-on-close, error routing (all `.code` paths), and retry — genuinely exercised, not just reasoned through** | **Cancel-on-close**, real: closed the Chat Panel 1.5s after submitting a question, while the call was still in flight. Log confirms the full chain: `Vision call starting` → `Chat Panel closed with 1 turn(s)` (not 2 — no phantom reply) → `Vision call cancelled (panel closed mid-request)`, 1ms after the close. **Error routing**, all 5 reachable paths tested directly against the real code (`askAboutRegion()`/`responseHandler.js`, stubbed `chatPanelWindow`) rather than assumed from the mapping table: `network` (closed port, real `ECONNREFUSED`) and `rate_limit` (local mock server, real HTTP 429) and both `malformed` shapes (mock server: non-JSON 200 body; valid JSON with empty `choices`) and `api_error` (mock server 500, **and separately the real Mistral endpoint with a deliberately invalid key, real HTTP 401** — the realistic version of this case) — every one produced the exact §7 message text and left `turns` at exactly the unanswered user turn, no phantom assistant turn in any case. Nothing sent to any third party — a local Node mock server stood in for Mistral except the one deliberate real-endpoint/bad-key case. **Retry**: scripted the exact flow `main.js`'s `onRetry()` runs — first call fails (turns stays at 1, error shown), guard confirms last turn is still `role:'user'` (matching the real guard condition), second call against a now-fixed endpoint succeeds and appends the assistant turn (turns → 2). All temporary test edits (a swapped `CHAT_COMPLETIONS_URL`, a throwaway mock server + test harness in `/tmp`) confirmed fully reverted — `visionClient.js` back to the real endpoint, no test artifacts left in the repo | Verified 2026-08-24 — this is what actually closes Step 7, not the row above |
| **"Incident B" — 3 consecutive gesture failures immediately after a completed Chat Panel session, investigated and left as unreproduced pre-existing flakiness, not a Step 7 regression** | **What was observed**, in a run I launched and controlled directly (not secondhand): a real 2-turn Chat Panel session (with a real `askAboutRegion()` call, real reply, the loading-state `inputEl.disabled`/`.focus()` cycle actually exercised) closed cleanly at `13:37:56.151`. Starting 8s later, three consecutive `Control+1` presses each produced **zero** `Digit1` keyboard events for the full 2.5s liveness window (`[safety-net] No trigger-key event at all within 2500ms`), despite the user confirming (correcting my initial "quick tap" assumption) that at least one of these was a genuine multi-second hold with the trail visibly drawing — meaning mouse events reached the overlay while keyboard events for the same hold did not. This is a different shape than the already-documented "released within ~445ms, blind window" mechanism, since a real multi-second hold should reach macOS's auto-repeat and keep the overlay non-blind well within 2.5s if focus was actually won. **Investigation**: added temporary diagnostic logging (`overlayWindow` focus/blur events, `isFocused()` reads at activation and at safety-net-fire time) to observe rather than theorize further. One of the two diagnostic lines called `app.isFocused()` — not a real method (that's a `BrowserWindow` instance method, not one on the `app` module singleton) — which threw an uncaught `TypeError` in a `setTimeout` callback on the next hotkey press and **crashed the whole Electron main process** (no `uncaughtException` handler exists to catch it). Fixed by replacing both calls with `BrowserWindow.getFocusedWindow()`. **Whether the crash fix was actually "the fix" for Incident B was explicitly reasoned through before accepting the retest results, not skipped past**: (1) chronologically impossible — the buggy diagnostic code didn't exist yet when Incident B occurred, so it cannot have *caused* Incident B; (2) mechanically, the diagnostic code is read-only (`.isFocused()`/`.isVisible()`/`.getTitle()`/passive `focus`/`blur` listeners) and never touches `app.focus({steal:true})`, `overlayWindow.focus()`, or the `globalShortcut` register/unregister calls that actually control focus and event routing — no plausible mechanism for a broken log statement to have been suppressing or fixing a real keyboard-delivery bug. **The control test** (explicitly requested rather than accepting the reasoning alone): reverted the diagnostics entirely — `main.js` back to exactly the pre-diagnostic Step 7 code, confirmed via `git diff` — then deliberately repeated the exact failing sequence (real multi-turn session exercising the loading-state cycle → close → immediate genuine multi-second-hold gesture) **three times in a row, zero diagnostic code present**. All three succeeded (one produced a legitimate "below minimum size" rejection, itself proof of real keyboard-event delivery, not a failure). Diagnostic reads also showed the synchronous `isFocused()` check immediately after calling `.focus()` is itself just racy (sometimes `false`, sometimes `true`, on both successful and unsuccessful-looking reads) — the real signal is the async `focus` event, which fired reliably (2-14ms later) on every attempt regardless. **Conclusion**: not reproduced under the exact conditions that produced it, with the diagnostics that couldn't have caused or fixed it removed entirely. Treated the same as this project's other single/few-occurrence anomalies (the hold-to-talk watchdog anomaly in decisions.md) — recorded, not claimed fixed, with a concrete revisit trigger below | Unreproduced 2026-08-24 (3/3 clean control-test cycles) — **revisit trigger: if this recurs again, especially tied to the loading-state disable/focus cycle specifically rather than gesture timing generally**, that would upgrade this from anomaly to pattern and warrant a real fix search, not another round of "probably flakiness" |
| **Step 8 storage: JSONL, not SQLite** — `node:sqlite` checked empirically first, not ruled out on the old packaging-risk grounds | todo.md/system_design_plan.md §3.7 left SQLite-vs-JSONL open. Checked `node:sqlite` (Node's built-in SQLite module) directly against the real bundled runtime rather than the system Node or the `engines` field: ran `ELECTRON_RUN_AS_NODE=1 ./node_modules/.bin/electron -e ...` and confirmed Electron 43.4.1 bundles **Node 24.18.1**, and `require('node:sqlite')`'s `DatabaseSync` loads and runs real `CREATE TABLE`/`INSERT`/`SELECT` correctly inside that exact process. Notable: the system Node (v23.9.0) emits `ExperimentalWarning: SQLite is an experimental feature` on every load (reproduced twice); Electron's bundled Node 24.18.1 does not, under an identical script with no explanatory env var difference — `--experimental-sqlite` still exists in `process.allowedNodeEnvironmentFlags` on both, so Node still tracks it as an experimental-category flag internally, but the runtime warning is gone by this version. This matters because it means `node:sqlite` carries **none** of the native-addon packaging risk that sank `whisper-node-addon`/complicated `nodejs-whisper` — it's compiled into the Electron/Node binary itself, not a separately installed/compiled artifact, so it structurally can't fail the same way. **Decision made on updated grounds, not the original (now-moot) risk argument**: JSONL still chosen, because (1) `data_pipeline.md`'s own curation workflow is hand-editing a bad answer directly in a text editor — JSONL-native, SQL-hostile; (2) nothing in current/near-term scope (Phase 2/3 as designed) does relational querying, and `turns` would serialize into a JSON TEXT column under SQLite either way, so its actual advantage sits unused; (3) zero schema/migration code now vs. a small but real ongoing maintenance surface for a benefit nothing currently needs; (4) no reason to adopt an internally-flagged-experimental API without an active need for it, even a well-behaved one. `node:sqlite` is recorded as **evaluated and viable, not ruled out for risk** — if querying needs ever materialize, or if Electron's bundled Node version is ever upgraded, re-verify its behavior/availability rather than assuming this measurement still holds (see todo.md) | Active — implemented 2026-08-24, `localLogger.js` |
| **Step 8: temp-file keep/discard split changed from `turns.length === 0` to "at least one complete exchange exists"**, with a dangling trailing turn trimmed before logging | Traced rather than assumed: `main.js` pushes a user turn onto `turns` *before* the model call starts, and only pushes the assistant reply on success (`responseHandler.js`). If the panel closes mid-request (§7's already-verified cancel-on-close), or the user never retries a shown error, `turns` is left ending on that **unanswered user turn** — length ≥ 1, not 0. Under the old `turns.length === 0` check this hit the *keep* branch, meaning a naive Step 8 write would have logged a broken conversation (a question with no answer), directly contradicting §7's "don't log a partial/broken conversation." decisions.md's own temp-file-lifecycle row had flagged this exact gap as open ("whatever the eventual don't-log-empty/broken-conversation rule ends up being"). **Fix**: `trimToCompleteExchanges(turns)` drops a trailing `{role: 'user'}` turn before the keep/discard decision and before logging — safe to assume the dangling turn can only ever be the *last* entry, never mid-array, because `turns` is strictly append-only (nothing in the codebase splices or removes from it). The keep/discard gate now checks the *trimmed* array's length, not the raw one, so a session that only ever produced one unanswered, cancelled question is treated exactly like a zero-turn session — deleted, not logged. A session with real answered turns followed by one dangling cancelled follow-up still gets kept and logged, just without that dangling tail. Verified via `diagnostics/verify_local_logger.js`: well-formed (ends-on-assistant) turns pass through untouched, a 3-turn array with a dangling trailing user turn trims to 2 and still ends on the real assistant reply, and an array of only one unanswered question trims to zero | Active — implemented and verified 2026-08-24, `main.js`'s `endChatSession()`/`trimToCompleteExchanges()` |

| **Theme mechanism: `nativeTheme.themeSource` + bare `prefers-color-scheme`, no `data-theme` attribute or IPC broadcast** | Electron's `nativeTheme.themeSource` directly controls what the `prefers-color-scheme` media feature evaluates to, app-wide, in every renderer — setting it to `'light'`/`'dark'` overrides the OS setting everywhere at once; `'system'` (the default) removes the override and lets the real OS appearance through. This means `src/shared/theme.css` only needs one `@media (prefers-color-scheme: dark)` block of token overrides, and `settings.js`'s Theme control only needs to set `nativeTheme.themeSource` — no per-window listener, no `data-theme` attribute, no IPC broadcast to propagate the change live, because Electron already does that propagation internally. Verified against Electron's own documented behavior for `themeSource`, not assumed from the token-swap pattern alone. Persisted to `settings.json` and re-applied at the top of `app.whenReady()`, before any window loads, so a relaunch doesn't flash the wrong theme | Active — implemented 2026-08-25, Phase 2.3 |
| **Icon webfont: vendored static files, not the `@tabler/icons-webfont` npm package** — a real course-correction on the approved "self-hosted" decision, not a silent swap | Self-hosting (over a CDN `<link>`) was approved for offline reliability. Installing `@tabler/icons-webfont` to get there turned out to cost far more than that goal needs: **121MB and 203 packages** added to `node_modules` (it declares `svgtofont`/`svg-path-commander`/the full `@tabler/icons` SVG source as runtime `dependencies`, not `devDependencies`, even though none of that is used at runtime — only `dist/tabler-icons.css` + one font file ever get loaded by the app). What the app actually needs is ~673KB: the minified CSS (211KB) and the regular-weight `woff2` (462KB) — no `woff`/`ttf` fallbacks needed, since Electron's bundled Chromium always supports `woff2` and this isn't a public site needing broad browser support. **What was actually done**: installed the package temporarily (`--no-save`) to extract exactly those two files into `src/shared/assets/tabler-icons/`, rewrote the CSS's `@font-face src` down to the single local `woff2` reference, then fully removed the npm package and its transitive tree from `node_modules` and confirmed `package.json`'s `dependencies` were never touched (`git diff` showed only `main.js`/`mainWindow*` changing, no `package.json` diff). This still satisfies "self-hosted, no CDN, no runtime network dependency" — it just gets there by vendoring the specific files instead of carrying the much heavier dependency that generates them | Active — implemented 2026-08-25, Phase 2.3. `src/shared/assets/tabler-icons/{tabler-icons.css,tabler-icons.woff2}`, linked from `mainWindow.html` |
| **Dictation language: pinned-language picker (English/Vietnamese/Spanish), not an English/Multilingual-auto-detect toggle** | Raised as a simplification (binary toggle, one new accuracy case to verify) and explicitly rejected on review: whisper's auto-detection is measurably less reliable on short clips, which is the norm for a hold-to-talk gesture — exactly the case this feature needs to work well for — and pinning doesn't cost more to verify than the toggle would have, since "per-language accuracy pass" already meant testing multiple specific languages either way. Same UI/verification cost, meaningfully worse transcription mechanism for the toggle — not a good trade. `settings.json`'s `dictationLanguage` is a language key (`'en'`\|`'vi'`\|`'es'`), not a boolean, so adding a fourth language later is a lookup-table entry, not a schema change. **A real API-shape finding along the way**, checked directly per the user's standing "verify, don't assume" bar (same one that caught `extractTranscriptText()`'s shape earlier): `nodejs-whisper`'s top-level `IOptions` has no `language` field at all — it's nested under `whisperOptions.language`, confirmed by reading `WhisperHelper.js`'s `constructCommand()` directly, which turns it into whisper.cpp's `-l <lang>` flag (`args.whisperOptions?.language \|\| 'auto'`). Passing `language` at the top level (the wrong place) would have silently done nothing — Whisper would default to `-l auto`, undermining the entire point of pinning a language. English keeps the existing `base.en` model (whisper.cpp's `.en` models are English-only regardless of `-l`); Vietnamese/Spanish use the multilingual `base` model (same size class as `base.en`, not jumped to `small`) with the language pinned explicitly. Model size is left as the knob a real accuracy pass tunes, not pre-decided | Active — implemented 2026-08-25, `src/shared/settings.js`'s `DICTATION_LANGUAGES`, wired into `main.js`'s `hold-to-talk-end` handler. **Each language still needs its own real accuracy pass before being called verified** (English unchanged from its existing Step 5 verification; Vietnamese/Spanish are new and untested) — tracked in todo.md, not assumed to work from the model swap alone |
| **Hotkey test mechanism (Settings' "Change hotkey" flow): reuses the production gesture/watchdog machinery via a `hotkeyTestMode` flag, rather than a parallel test harness** | Proposed and approved with two additions on review. **Core design**: `activateGestureOverlay()` was extracted verbatim from `registerHotkey()`'s own callback (display positioning, `activate` IPC, show/focus, arming the watchdog) so a hotkey test's candidate accelerator drives the exact same real gesture path a production press does — `startHotkeyTest()` just registers the candidate and arms `hotkeyTestMode = {accelerator, triggerKeyCode, sender}`; the real gesture happens when the user does the hold-drag-release themselves. `selection-finalized` and the watchdog's timeout branch both check `hotkeyTestMode` first: a valid bbox → **pass** (revert to production immediately, candidate isn't live until a separate Save click); a below-minimum bbox → **inconclusive**, re-arm the same candidate for another attempt without reverting (`rearmHotkeyTest()`) rather than making the user re-click Test — mirrors how a real gesture already just gets silently discarded on too-small, not treated as an error; watchdog timeout → **fail**, revert to production and report `timeout`. `globalShortcut.register()` itself returning `false` (OS/another-app conflict) is caught before any gesture is attempted at all (`already-in-use`). **Addition (a), approved on review**: a static pre-check in the Settings renderer blocks recording an Option/Alt-containing combo *before* it's ever sent to main.js for a live test — decisions.md already established this is a guaranteed failure (macOS's text-input/composition layer eats the trigger key), not a maybe, so no reason to spend a real gesture-test cycle confirming it again. Every other known failure class (OS-reserved combos, input-method interception like `Control+Space`, chord-timing order) has no static signature and still only gets caught by the real gesture. **Addition (b), a genuine fork raised on review**: does the production hotkey stay live during a test? Two options were weighed — keep production registered by giving the test its own fully separate watchdog/overlay state (avoids any disruption, but duplicates exactly the machinery this design's whole point was to reuse, not fork), or unregister production for the test's bounded duration, reusing the single-flight state as-is (zero new state, but the production hotkey is briefly unavailable while a test is in flight). Chosen: **unregister for the bounded duration** — the gesture/watchdog state this reuses (`stuckGestureTimer`, `seenTriggerKey`, `activeDisplayId`, the one `overlayWindow`) only ever holds one gesture's worth of bookkeeping, so a candidate test and a real production gesture genuinely can't run through it at once without colliding; and the exposure is identical in shape to what a real gesture already costs today (~2.5s/5s watchdog window), not a new risk. Explicitly distinguished from the concurrent-session-guard precedent that rejected a similar-sounding "unregister for a while" design at Step 5 (decisions.md, 2026-08-22): that case was an *unbounded*, *everyday* interaction (any Chat Panel session) where a silently-dead hotkey would be a real ongoing cost; this is a *narrow*, *self-initiated* window inside a Settings flow the user deliberately opened — not the same mistake recurring. **UI addition, cheap, no new state**: the Settings test panel shows an explicit "your hotkey is paused while testing" status line for the test's duration — this project already learned once (the Dock-icon and the accelerator-claims-the-keyup findings) that a silent gap reads as broken even when nothing actually is; the message covers that without touching the state-reuse design underneath it | Active — implemented 2026-08-25, `main.js` (`activateGestureOverlay`/`startHotkeyTest`/`rearmHotkeyTest`/`hotkeyTestMode`), `mainWindow.html`'s hotkey recorder + test panel |
| **Phase 2.3 (Settings page + shared theme layer) — implemented, not yet hands-on verified** | Built per the plan proposed and confirmed before implementation (see the four rows above for the pieces that changed on review). Smoke-tested only: `npm start` launches with no crash, `Control+1` still registers cleanly post-change, no lingering process after kill (`ps aux` clean). **What smoke-testing cannot cover, and genuinely hasn't been exercised yet**: clicking through the Settings UI itself (theme radios, display name field, dictation dropdown, data/privacy buttons), and — the highest-stakes one — an actual hotkey-recording → live-gesture-test → Save cycle with real hands and a real hold-drag-release, the same "registration success proves nothing" bar this project has applied to every accelerator choice so far. None of that can be driven synthetically (`globalShortcut`/real OS keyboard focus, per the `run` skill's established finding, confirmed again at Step 8's real-session verification) — needs the user's own hands, same as every other real-gesture verification in this project | Implemented 2026-08-25 — **hands-on verification still open**, see todo.md |

| **`startHotkeyTest()`/`rearmHotkeyTest()`'s missing `unregisterAll()` — confirmed root cause and fixed** | Real hands-on hotkey-test verification (2026-08-25) surfaced a genuine bug, not a testing artifact: a real hold-drag-release on a freshly-recorded candidate drew the trail normally but never finalized on release, hanging until the watchdog timed out. Traced to the exact mechanism decisions.md's consolidated hold/release section already documents ("the accelerator must be unregistered during the gesture, or macOS's global-hotkey layer claims the trigger key's keyup exclusively") — `startHotkeyTest()`'s registered callback (and `rearmHotkeyTest()`'s identical one) had been copied from `activateGestureOverlay()`'s activation logic but never included the `globalShortcut.unregisterAll()` call production's own `registerHotkey()` callback makes right before activating. Fixed by adding the missing call inside both callbacks, in the same place production has it. **Re-verified with a real Control+2 candidate**: fired, held, released — finalized cleanly on the real keyup, no timeout (`[hotkey-test] Control+2 passed — real gesture finalized.`), Save persisted it to `settings.json`, the new accelerator activates the overlay and the old one (`Control+1`) does nothing | Confirmed and fixed 2026-08-25 |

| **Missing logging on the hotkey-test path — added, matching `registerHotkey()`'s existing pattern** | While diagnosing an unrelated accidental activation (the row below), it became clear `startHotkeyTest()`'s/`rearmHotkeyTest()`'s registered callbacks and the `hotkey-test-start`/`hotkey-test-cancel`/`hotkey-save` IPC handlers had **no log statements at all** — every other path through the hotkey/gesture-activation code in this file logs immediately, first thing, specifically because this area has cost more debugging time than any other in the project's history. The gap directly cost real diagnostic time: a genuine overlay activation happened with zero identifying log line, leaving only a reconstructed-from-memory account of what triggered it until the logging was added and the sequence re-run for a clean trace. Fixed: each callback/handler now logs immediately on entry, same style as `registerHotkey()`'s `Hotkey pressed [...]` line (e.g. `[hotkey-test] Candidate [...] fired — waiting on keyup of ...`, `[hotkey-test] Start requested: ...`, `[hotkey-test] Cancelled: ...`/`no test was running`, `[hotkey-test] Save requested: ...`) | Active — implemented 2026-08-25, directly enabled the finding below |

| **Command (Cmd/Meta) chord failure — Command+3 specifically confirmed to fail on release; NOT yet generalized to "Command as a modifier class"** | With the new logging in place, a real hold-drag-release test on a `Command+3` candidate produced a clean, unambiguous trace: `[hotkey-test] Candidate [Command+3] fired` at press, then `[safety-net] No trigger-key activity for 5000ms — force-closing` — the 5000ms (not 2500ms) branch, meaning real keydown activity (`seenTriggerKey: true`) was seen before the release specifically failed to arrive. Same *shape* as Option's already-documented failure (fires, focus is real, release never reaches the window) — **but not the same evidentiary footing**. Option's block is grounded in five separately-tested combos plus a mechanistically understood, chord-independent cause (the OS composition/text-input layer intercepts Option regardless of which key it's paired with — Option+Space, Option+D, etc. all fail identically). Command+3 is currently **n=1**. Ruled out: a competing third-party app claiming it (the app was frontmost and focused when it fired — nothing else could have raced for it) — but that only eliminates *one* possible cause, not "macOS itself reserves this specific combo," which is a live alternative given this project's own precedent (`Control+Space` failed for a combo-specific OS-level reason — the input-source switcher — not because `Control`-the-modifier is broadly unsafe; the same shape could apply here, i.e. macOS reserving `Command+3` specifically rather than `Command` generally being unsafe). **Disambiguating control test already run**: `Shift+3` (same trigger key, different modifier) on the identical candidate-test code path finalized cleanly (`Shift+3 passed — real gesture finalized`, ~3.8s real hold) — confirming this is not a general test-mode defect, isolating the variable to the modifier. **Resolved 2026-08-25 — second combo tested, block added.** `Command+7` was tested next (deliberately a different key, no a priori collision). The log showed 3 attempts, and reading them precisely mattered: attempts 1 and 2 hit the *other* watchdog branch (`No trigger-key event at all within 2500ms` — zero keydowns ever seen), which is the already-documented, accelerator-*independent* quick-tap limitation (releasing inside the ~445ms pre-auto-repeat blind window) — not evidence about Command, just fast taps, noise for this specific question. Attempt 3 was a genuine multi-second hold (`seenTriggerKey: true`, the 5000ms branch) and failed identically to `Command+3` — release never arrived. **Net result: n=2 independent Command combos (different keys), both genuinely held past the blind window, both failing the same way on release.** Named honestly per the bar set before testing: this is real repeated-pattern evidence, but still short of Option's five-combo, mechanistically-understood footing — not being overstated as equivalent confidence. **Decision, made on this evidence**: extend the static pre-check (`mainWindow.html`'s `onRecordKeydown`) to also block Command/Meta at record time, same treatment as Option/Alt — a known, repeated failure shouldn't cost a live-test cycle to rediscover. User-facing message states the actual reason (two Command combos failed release delivery in testing) rather than a generic "not supported." **Revisit trigger, explicit**: if a Command combo is ever found to pass the live test cleanly, this block is wrong and should come back off — it was built on n=2, not a proven chord-independent mechanism the way Option's was | Active — implemented 2026-08-25 |

| **Chat Panel hold-to-talk silently broken by the Settings hotkey-change feature — hardcoded `'Digit1'`, found and fixed** | Real hands-on Phase 2.3 testing (2026-08-25): after changing the production hotkey to `Control+2`, holding it in the Chat Panel started a recording (mic genuinely opened, audio genuinely captured) but never produced a transcript — traced to `chatPanel.html`'s keydown/keyup listeners being hardcoded to `e.code !== 'Digit1'`, a holdover from when the hotkey was permanently `Control+1` and never changeable. `main.js`'s `registerHotkey()` correctly detects the chat-panel-focused case and starts the flow, but `chatPanelWindow.webContents.send('hold-to-talk-start')` carried no payload — the renderer had no way to learn the *current* trigger key. Net effect: neither the liveness pings nor the real keyup ever matched, so every hold-to-talk attempt silently died at the watchdog's 2.5s "no trigger-key event at all" branch (audio discarded regardless of what was said), for **any** changed hotkey, not just this specific one. **Fix**: thread `triggerKeyCode` through `hold-to-talk-start` the same way the Overlay's `activate` payload already does — `main.js` sends it, `chatPanelPreload.js` forwards it, `chatPanel.html` stores it in a `triggerKeyCode` variable (mirroring `overlay.js`'s own pattern) instead of a hardcoded string, both listeners check against that. Confirmed the fix by grepping for every other `Digit1` occurrence in the codebase — no other hardcoded instance found, only comments and the `DEFAULTS`/`ACCELERATOR_CANDIDATES` fallback value (correctly overridden by `settings.json` at startup, not a bug). Re-verified working immediately after the fix: real English dictations transcribed correctly on the changed hotkey | Confirmed and fixed 2026-08-25 |

| **`nodejs-whisper`'s `autoDownloadModel()` is broken under Electron — `shelljs.exec()` returns `undefined`, worked around by pre-downloading models directly, not fixed in our code** | Switching Vietnamese/Spanish to the `base` multilingual model (needed since it had never been auto-downloaded, unlike `base.en`) failed with `TypeError: Cannot read properties of undefined (reading 'code')` inside `autoDownloadModel.js`, at the line checking `shelljs.exec(...).code` — meaning `shelljs.exec()` itself returned `undefined` rather than a result object, inside Electron's main process. **Isolated, not assumed**: ran the exact same `download-ggml-model.sh base` script directly from a plain shell (bypassing `nodejs-whisper`/`shelljs` entirely) — it worked perfectly, real download, exit code 0, correct file (`ggml-base.bin`, matching `MODEL_OBJECT.base` exactly). This confirms the failure is specific to `shelljs.exec()`'s behavior under Electron's Node runtime, not the script, the network, or our own code. **Workaround, not a fix**: manually ran `download-ggml-model.sh` directly for each newly-needed model (`base`, later `small` — see the row below); once a model file already exists on disk, `autoDownloadModel()` checks for it first and returns early, skipping the broken `shelljs.exec()` path entirely, so this doesn't need to be a permanent code change to keep working *on this machine*. **Real, unresolved packaging risk this surfaces**: this is a **second, independent** way a fresh install could fail to get a model it needs, distinct from the already-tracked "Phase 7 must bundle a precompiled `whisper-cli` binary, not compile from source" checklist item (todo.md) — that one covers the *binary*; this one covers *any model not already present*, including a user simply switching Settings' dictation language to something not yet downloaded on their machine. Not investigated further (which `shelljs` version, why `exec()` returns `undefined` specifically under Electron) — out of scope for closing Phase 2.3, but a real gap for Phase 7 packaging to account for | Confirmed 2026-08-25 — workaround applied, root cause in `shelljs` unresolved, **added to the Phase 7 pre-packaging checklist in todo.md** |
| **Vietnamese/Spanish dictation accuracy: `base` model confirmed genuinely inadequate, upgraded to `small` — decided only after two cheaper checks ruled out simpler causes** | Real hands-on testing surfaced garbled Vietnamese output on `base` (e.g. `"Chào tối bức bộ đại răng sửa niềm kì."` — not accent/spelling errors, structurally wrong words). Before assuming model size was the cause and paying its real cost (a 466MB download, slower inference), two free checks were run first, per explicit instruction not to default to the model swap: **(1) Confirm `-l vi`/`-l es` is genuinely reaching whisper-cli**, not just assumed correct because the output "looked" Vietnamese-shaped (Whisper's own auto-detect is good enough from phonemes alone that a silent fallback to `-l auto` could produce the same surface-plausible-but-wrong result). Temporarily wired `logger.debug` through (previously a no-op) to capture `nodejs-whisper`'s own `[Nodejs-whisper] Executing command: ...` log line **and** whisper.cpp's own independent runtime confirmation (`lang = vi`, `lang = es` in its `main: processing ...` line — whisper.cpp reporting its own actual state, not our code's claim about it). Confirmed correct on every attempt; reverted the temporary debug wiring once confirmed. **(2) Confirm short clips (hold-to-talk's natural length) aren't the dominant factor** — a well-documented independent Whisper weak point. Real attempts ranged 2.0–3.8s initially; a deliberate near-5-second attempt (4.9s) was still comparably garbled (`"Hãy trở thành tối bức, các giây đoàn xây rạng của anh."`), which is real evidence against length being the primary driver, not just an assumption ruled out on priors. **Both checks came back clean/negative** — the flag was correctly wired, and length wasn't the dominant cause — which is exactly the condition set in advance for proceeding with the model swap. **Result, on real before/after data**: `base` produced pure word-salad; `small` on the identical ground-truth sentence (`"Hãy cho tôi biết ở đây đang xảy ra điều gì?"`) produced `"Hãy cho tôi biết hôm nay đang xảy ra điểm gì"` and `"Hãy cho tôi biết ở đây đang xảy ra tiếng gì"` — structurally correct sentences with one or two word substitutions, ordinary ASR error rather than nonsense. Spanish (`"Hola, ¿cómo estás?"`) and English (`"The quick brown fox jumps over the lazy dog"`, the unchanged `base.en` regression check) both came back as **exact matches** on `small`/`base.en` respectively. `DICTATION_LANGUAGES` in `settings.js` updated: `vi`/`es` now use `small` (`en` unchanged). Model pre-downloaded directly per the row above, sidestepping the `autoDownloadModel()` bug | Active — implemented and verified 2026-08-25 with real before/after transcripts for all three languages; this closes Phase 2.3's per-language dictation accuracy requirement |

## How to add a decision here

One row: what was decided, why (briefly), and current status (Active / Dropped / Open question / Superseded). If a decision gets reversed later, don't delete the old row — mark it Superseded and add the new row, so the history of *why* something changed is preserved.