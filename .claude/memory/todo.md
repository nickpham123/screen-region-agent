# To-Do

Concrete next actions, not aspirational plans (those live in `implementation_plan.md`). Keep this short — if it grows past ~10 items, it's turning back into a plan document; prune or move stale items to phase_log.md's log section.

## Now — Electron scaffold (per system_design_plan.md's verification-step plan)

- [x] Step 1: Scaffold minimal Electron app (main process + one window) → verify: app launches, shows a blank window
- [x] Step 2: Add `globalShortcut` hotkey press listener + immediate focus-grab on the overlay window → verify: pressing the key while any app is focused brings up a (blank for now) overlay that has keyboard focus
- [x] Step 3: Add window-scoped keyup detection on the overlay + mouse drag-to-select while the key is held, plus a "ready to select" badge on activation → verified: badge shows on activation **and persists until genuine movement**; dragging draws a freeform trail; releasing the key (not the mouse) finalizes and logs bbox coordinates. **Badge implementation is covered by this checkbox — there is no separate open badge item.** Notes (refreshed 2026-08-21): the live bbox-preview rectangle was superseded by click-to-reset (see decisions.md) and is no longer rendered; hotkey is **`Control+1`** (was `Alt+Space`, which was broken — see decisions.md); releasing the trigger key inside the ~445ms pre-auto-repeat blind window still loses the gesture, now bounded to ~2.5s rather than 5s
- [x] **Step 4: Add `desktopCapturer` + crop logic** → verified: releasing the drag saves a cropped PNG of that exact region, confirmed on both displays
      - Note: Step 3 is closed, reviewed and merged to `main`. Commits: `e9fa2ba` (Step 3 verified), `4ee443b` (single-instance lock), `5387246` (overlay follows cursor's display + Spaces fix), `85b321c` and `34dbad5` (memory/doc corrections). Nothing from Step 3 is outstanding.
      - Note: this step triggers macOS's Screen Recording permission prompt — the first permission this app asks for. Worth handling the not-yet-granted case rather than assuming it succeeds.
      - **Note (highest risk in this step): the crop must be verified on BOTH displays.** The overlay emits bbox coordinates in the *renderer's* coordinate space, which was measured to disagree with `getCursorScreenPoint()` by a few px, and the overlay now moves between displays with very different scale factors (built-in Retina, 1728pt logical / 2x; external QHD 2560x1440 / 1x). `desktopCapturer` works in physical pixels. Confirm the captured crop matches what the user drew on **each** display — an overlay that appears in the right place does not guarantee a crop that lands in the right place. This check was specified during Step 3 review but was impossible to run then, since nothing captured pixels yet.
      - **Design chosen (2026-08-21, see decisions.md): full-resolution capture, convert bbox by `scaleFactor` once inside `captureRegion(bbox)`.** Rejected a thumbnail-matched capture (avoids conversion math but trades away image fidelity — bad fit for the fine-tuning accuracy goal) and per-sample conversion (same formula relocated, no real benefit).
      - [x] Run the standalone verification script (`diagnostics/verify_capture_main.js`) — **done 2026-08-22, all assumptions confirmed on both displays** (scaleFactor formula, unshaped constraints, canvas sizing, source↔display mapping via `display_id`). See decisions.md for the numbers.
      - **New implementation constraint from that run**: read capture dimensions from `video.videoWidth/videoHeight`, **never `track.getSettings()`** — the latter was measured returning stale (previous-stream) dimensions on the second capture. See decisions.md's `getSettings()` row.
      - [x] Real `captureRegion(bbox, displayId)` implemented — **2026-08-22**, `capture.js` + additions to `preload.js`/`overlay.js`/`main.js`. Full-resolution capture, bbox × `scaleFactor` conversion, dimensions read from the video element (never `getSettings()`, see decisions.md), display identity threaded from activation rather than re-derived. Trail/badge/reset-flash contamination during capture handled with a deterministic content-clear in `overlay.js`'s keyup handler (not a settle timer — a first attempt using one was proposed-after-the-fact, rejected on review, and replaced; see decisions.md). Smoke-tested only (`npm start` launches, hotkey registers, no crash) — no real gesture has been run against it.
      - [x] **Built-in display crop correctness verified — 2026-08-22.** Three real gestures run; I read the actual `crop.png`/`full.png` pairs and cross-checked by converting each bbox to physical pixels (× scaleFactor) and confirming that region of `full.png` matches `crop.png`'s content. One gesture landed on the live-chat disclaimer text and matched exactly, including the specific mid-word truncation — strong positive confirmation, not just "looks plausible." The other two landed on blank/solid-color areas (weak but non-contradicting). No trail/badge/reset-flash contamination visible in any of the six images — the deterministic-clear fix held up in practice. Distinct viewer counts across the three `full.png` files also confirm each capture is genuinely fresh, not cached/reused.
      - [x] **External QHD display crop correctness verified — 2026-08-22.** One gesture, cursor on the external monitor: `full.png` confirmed to be that display (2560x1440, scaleFactor 1, matching the earlier diagnostic measurement) showing VS Code with a terminal panel open. The crop landed on real terminal log text and matched it **exactly, across six lines, character-for-character including the left-edge word truncation** — the strongest confirmation of the whole verification pass, and on the display (1x) that had zero coverage until this gesture. **Step 4's highest-risk item (crop correctness across two displays with different scaleFactors) is now closed on both displays.**
- [x] **Step 5: Add the Chat Panel — core loop done and hands-on verified 2026-08-22/23.** Text field auto-focused, message history list (starts empty, populates on submit), same key held again (window-scoped) triggers `nodejs-whisper` recording (backend switched from `whisper-node-addon` after it was confirmed broken — see decisions.md) → verified: typing + Enter adds a message to history; holding the key and speaking transcribes into the field with no network call made (beyond the one-time model download). See the sub-items below for the full verification trail.
      - [x] **Window lifecycle + temp-file lifecycle invariant — implemented 2026-08-22, hands-on verified 2026-08-23.** `createChatPanelWindow()`/`endChatSession()` in `main.js`, `chatPanel.html` placeholder. Fresh `BrowserWindow` per session (approved over reusing one window like the overlay — see decisions.md), `setVisibleOnAllWorkspaces` set once per creation. Concurrent-session guard revised after review (see decisions.md): hotkey stays *registered* throughout the session; its callback focuses the existing panel instead of starting a new gesture if one's already open **and doesn't have focus — see the hold-to-talk checkbox below for the focused case**. Close handler moves-then-would-log or deletes-and-discards per decisions.md's invariant. `moveFile()`'s rename/EXDEV-fallback logic verified standalone (`diagnostics/verify_move_file.js`). **Verified hands-on**: 4 real gestures across both displays (built-in + external Dell), panel opens and takes real keyboard focus every time; re-pressing the hotkey while a panel is open correctly focuses it instead of starting a second gesture (3/3); every close correctly discarded its temp files, confirmed on disk, not just in the log. **Still needed, not part of this checkbox at the time**: the text field, message history, actual audio capture, and their remaining preload/IPC wiring below, and Step 8's real `logConversation()` write (both since done — see Step 8 below). **Gap closed 2026-08-23**: the "keep" (move-to-permanent) branch fired for real for the first time — 3 real turns submitted via the text field, panel closed, log showed `Chat Panel closed with 3 turn(s) — moved to permanent storage`. Verified on disk, not just the log: files present in `userData/captures/` at byte-identical sizes to what was in temp (confirming content survived the move intact), temp folder empty afterward (a genuine move, not a copy), and the crop image opened cleanly with real, legible content.
      - [x] **Hold-to-talk key mechanics (not the actual audio capture) — implemented and hands-on verified 2026-08-23.** Investigated before implementing, per review: does `Control+1`, left registered under the guard above, still claim `Digit1` even once the Chat Panel has real focus? Verified empirically (temporary diagnostic, since replaced) rather than trusting the note below, which turned out to be wrong in its specifics — see decisions.md's "Hold-to-talk vs. the accelerator" row for the full chord-timing evidence. Fix: `registerHotkey()`'s guard now branches on `chatPanelWindow.isFocused()` — focused starts hold-to-talk exactly like a region-selection gesture (`unregisterAll()`, bootstrap the renderer's held-state from `'hold-to-talk-start'` rather than a local `keydown`, a dedicated liveness watchdog, end strictly on the real `Digit1` `keyup` via `'hold-to-talk-end'`, never a timeout), not-focused keeps the existing "just focus the panel" behavior. **Verified hands-on**: 3 real hold cycles (~3.1s, ~8s, ~2.2s) in one session, all clean — start → no watchdog force-close (the 8s hold only survives if the liveness pings are genuinely working) → end by real keyup → clean re-registration confirmed by a normal gesture working right after. **Not built**: the actual whisper-node-addon recording/transcription — `chatPanel.html` has two `TODO(next slice)` markers at exactly the start/end points where it plugs in.
      - Implementation note: reuse the `spaceHeld`-bootstrap pattern from Step 3 (see decisions.md) — a key already down when a window gains focus won't fire a local `keydown`, so bootstrap "held" state from the activation signal itself, not from waiting on `keydown`. **Done above** — applied via `'hold-to-talk-start'`, not a local `keydown`.
      - Implementation note (2026-08-21): hold-to-talk reuses Step 3's accelerator, now settled as **`Control+1`**, and inherits its constraints — **no Option in the chord** (the text-input layer eats the keyup, as it did for `Alt+Space`), and the modifier must go down before the trigger key. Also note the bootstrap pattern above is a first-instant workaround, not the whole story: a held key does emit auto-repeat `keydown`s (~80ms apart) once focus lands, which is what the Step 3 liveness watchdog runs on and is available for hold-to-talk too. **Superseded in its specifics 2026-08-23**: this note assumed hold-to-talk's key would just work normally via window-scoped events once the panel had focus — it does not, `Digit1` is claimed by the accelerator the same way it is for a gesture, and the fix above (unregister + bootstrap + watchdog, exactly the mechanism this note already correctly anticipated for a *different* reason) was needed for exactly that reason instead.
      - [ ] **Follow-up flagged on review (2026-08-23), not yet done**: all 3 hold-to-talk verification cycles ran in one session, on whatever display that session's one capture landed on — not positively confirmed which, and not exercised on the external Dell monitor specifically. Worth doing deliberately rather than assumed-fine-by-association: the earlier "panel takes real focus on the external display" verification (previous checkbox) predates `isFocused()` being read at all in this codepath, since that branch didn't exist yet when that test ran — so it confirms the panel *can* take focus there, not that this specific `isFocused()` check behaves correctly there. Do one real hold-to-talk cycle with the panel on the external monitor and confirm the same log sequence (`starting hold-to-talk` → no watchdog force-close → `ended by real keyup`).
      - [ ] **Follow-up flagged on review (2026-08-23), not yet done**: no `blur` listener exists on `chatPanelWindow` — confirmed by reading the code, not assumed. If OS focus leaves the panel mid-hold (a notification, switching apps) while `Control+1` is still down, the renderer stops receiving any further key events for it, including the eventual real `keyup`, so the session only ever ends via the generic silence watchdog (~5s after the last liveness ping) rather than anything focus-aware. Bounded — it self-recovers, doesn't hang forever — but for up to that ~5s window, `Control+1` is dead everywhere (not just in the panel) with no feedback, since the accelerator stays unregistered until the watchdog fires. This was an **identified gap on review, not a considered-and-accepted one** — no blur handling was designed for during implementation. Candidate fix, not built: a `blur` listener on `chatPanelWindow`, armed only while `holdToTalkActive`, reacting immediately instead of waiting on the generic timeout. **Unconfirmed candidate observation, 2026-08-23 — needs reproduction, not a settled finding.** The very first hold-to-talk test that session hit this shape — watchdog force-close at 5s instead of a real keyup — on what was also the app's first-ever `getUserMedia()` call, which would plausibly trigger a real macOS mic permission dialog. That dialog stealing focus mid-hold is *one* explanation consistent with the symptom, but it is not confirmed: `chatPanelWindow.isFocused()` was never instrumented during that attempt, whether a permission dialog actually appeared was never checked, and the event has not recurred since (3 more attempts in that same session and 5 more across later sessions all ended cleanly on real keyups). Equally-plausible unruled-out alternatives exist too — e.g. the key simply not being held continuously on an unfamiliar first attempt. Treat this as a single anomalous data point, not evidence the gap has "already cost a real dictation" — that overstates what's known. The underlying gap (no `blur` listener) remains real and worth fixing eventually regardless of whether this one incident was its cause; if it recurs, that would upgrade this from anomaly to pattern.
      - **Implementation note (2026-08-22) — the close handler owns the temp-file lifecycle invariant**: at panel-close, exactly one of {move crop.png/full.png out of temp into permanent storage, then log} or {delete temp files, don't log} must happen — never neither, never both. **Done above.** Full design (including the explicitly-rejected calendar-auto-delete alternative and why) is in decisions.md's "Temp-file lifecycle" row, and see Step 8 below for the actual `logConversation()` write, since implemented.
      - **Implementation note — the Chat Panel will need `setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true })`.** **Done above.** It opens after capture and must take keyboard focus for typing. macOS runs a separate Space per display, and a window shown on a non-active display renders and takes *mouse* events but never becomes the *key* window — so typing silently does nothing. This was hit and fixed for the Selection Overlay (see decisions.md); the Chat Panel is the next window that can appear on a secondary display, so apply it proactively rather than rediscovering it. Status of that finding: **which line fixes it is established by single-variable experiment** (0/5 gestures worked without it, 4/5 with it); **why it works is inferred** — `isFocused` was never instrumented during the failure. **Re-verified for the Chat Panel specifically, 2026-08-23** — real hands-on session on the external Dell monitor confirmed the panel took keyboard focus there too, same as the overlay's earlier verification.
      - [x] **Text field + message history — implemented and hands-on verified 2026-08-23.** `chatPanel.html`/`chatPanelPreload.js`: `#input` auto-focused, Enter submits a `{role:'user', content}` turn, rendered into `#history` and sent via `chatPanelAPI.submitTurn()` to `main.js`'s already-scoped `chat-turn-added` listener (unchanged — just never had a real sender until now). **Verified hands-on**: 3 real turns typed and submitted in one session, panel closed, log showed `Chat Panel closed with 3 turn(s) — moved to permanent storage` — **the first time the "keep" branch has ever fired end-to-end** (previously only covered by `diagnostics/verify_move_file.js`'s standalone unit test, tracked as a gap since 2026-08-23). Confirmed on disk, not just the log: files present in `userData/captures/` at byte-identical sizes to temp (content survived the move intact), temp folder empty afterward (genuine move), and the crop image opened cleanly with real, legible content.
      - [x] **Real voice recording/transcription implemented 2026-08-23** — see the "`nodejs-whisper` wired into the two `TODO(next slice)` points" item under "Open questions blocking later phases" below for the full design and what's confirmed vs. still needs hands-on testing. Verify per this item's top-level criteria once tested: holding the key and speaking transcribes into the field with no network call made (beyond the one-time model download, already done).
- [x] **Step 6: Port `askAboutRegion(imagePath, conversationHistory)` to JS — implemented and verified 2026-08-23** (`visionClient.js`, `diagnostics/verify_visionclient.js`). Scoped down to the core crop-image + multi-turn-history call on explicit review sign-off (see decisions.md) — the thumbnail/tool-escalation/prompt-caching notes below are tracked as a separate follow-up, not folded in. Verified against a real crop on disk and the real Mistral API: single-turn call returned an accurate description of the actual image; a follow-up call against the growing history returned a coherent, consistent answer (not a fresh unrelated one), confirming history round-trips; missing-API-key case threw a clear error. Failures surface as `Error`s with a `.code` (`network`/`rate_limit`/`api_error`/`malformed`) for Step 7's Response Handler to route to §7's messages — this module formats no UI text itself. Not yet wired into the live Chat Panel — that's Step 7.
- [ ] **Follow-up, deferred from Step 6 (see decisions.md's scope-decision row) — do before Step 7's wiring**: extend `visionClient.js` with the full-screenshot hybrid context approach — a downscaled thumbnail sent alongside the crop on every call, plus a `requestFullScreenshot()` tool the model can invoke for full-resolution context when the thumbnail isn't enough (system_design_plan.md §3.5) — and a stable `prompt_cache_key` per session (keep the prefix — system prompt + image + prior turns — byte-identical across calls, only append the new turn) to cut repeated-content token cost. Deliberately built and verified as its own pass, not simultaneously with the base multi-turn call.
- [x] **Step 7: Wire Chat Panel submit → Response Handler → Vision Model Client → append answer to history — implemented and fully verified 2026-08-24** (`responseHandler.js`, plus wiring in `main.js`/`chatPanel.html`/`chatPanelPreload.js`). Core loop verified twice with real multi-turn sessions (accurate, specific replies; loading indicator visually confirmed by the user). Cancel-on-close and all `.code`-based error paths (network/rate_limit/malformed×2/api_error×2) plus retry were initially built but NOT tested — caught on review before accepting "Step 7 complete" — then genuinely exercised: real cancel-mid-request confirmed via log chain, all 5 error paths hit real classification code (local mock server + one real-endpoint/bad-key case) each producing the exact §7 message with no phantom turn, retry confirmed to actually re-send and succeed. A real regression scare ("Incident B" — 3 consecutive gesture failures right after a completed chat session) was investigated, a self-inflicted crash bug (`app.isFocused()` isn't a real method) found and fixed along the way, and — after an explicit no-diagnostics control test requested on review rather than accepting reasoning alone — left as unreproduced (3/3 clean) rather than a confirmed Step 7 regression. Full evidence trail in decisions.md.
- [x] **Step 8: On panel close, write the full turn history via Local Logger — implemented and verified 2026-08-24** (`localLogger.js`, plus `main.js` changes). Storage is JSONL (`userData/conversations.jsonl`, one line per conversation) — `node:sqlite` was checked empirically against Electron 43.4.1's real bundled runtime (Node 24.18.1) and confirmed viable with no native-addon risk, but JSONL still chosen; see decisions.md for the full reasoning and the row below for the re-verify trigger. `endChatSession()`'s keep/discard branch changed from `turns.length === 0` to "at least one complete exchange exists" (`trimToCompleteExchanges()`), fixing a real gap: a session cancelled mid-request left a dangling unanswered user turn that the old check would have logged as a broken conversation, contradicting §7. Verified via `diagnostics/verify_local_logger.js` (trim logic's three cases, and `logConversation()` appending one well-formed §5-shaped JSONL line), **and via a real end-to-end Chat Panel session for the happy path** (see the checkbox below) — the dangling-turn trim path specifically remains diagnostic-only, see the dedicated item below. Implementation note (2026-08-22, now resolved): `logConversation()` must never write a path still inside `app.getPath('temp')` — confirmed it doesn't; `endChatSession()` moves `crop.png`/`full.png` to `permCropPath`/`permFullPath` before calling `logConversation()`, never the temp paths.
      - [x] **Real end-to-end verification — done 2026-08-24.** A real `Control+1` gesture → 3 real typed submit→reply exchanges (6 turns) → real panel close. Confirmed on disk: `conversations.jsonl` has one line matching §5's schema exactly, permanent crop/full PNGs exist with real correct content at the paths the record points to, temp folder empty afterward. This closes Phase 1's designed 8-step plan for the happy path.
      - [x] **`trimToCompleteExchanges()`'s dangling-turn trim verified via a real mid-request panel close — done 2026-08-24.** Real gesture, first question submitted and its real reply received (2 turns), a follow-up submitted (3 turns, `Vision call starting (3 turn(s) in history)`), panel closed immediately — before the reply arrived. Log confirmed the trim before the cancel even settled: `Chat Panel closed with 2 turn(s) — moved to permanent storage and logged`, then `Vision call cancelled (panel closed mid-request)`. Confirmed on disk, not just the log: `conversations.jsonl`'s new record has exactly the 2 real turns from the answered exchange — the dangling, unanswered follow-up question is genuinely absent from the array, not merely uncounted. Permanent files present with correct content, temp folder empty afterward. This is the last Step 8 path that hadn't had real-usage verification — now closed.
      - [ ] **If `node:sqlite` is ever revisited, or if Electron's bundled Node version is ever upgraded**: re-verify its behavior/availability via `ELECTRON_RUN_AS_NODE` against the actual bundled runtime rather than assuming 2026-08-24's measurement (Node 24.18.1, no experimental warning) still holds — see decisions.md's Step 8 storage row for the full empirical method to repeat.

## Cleanup

- [ ] Delete the superseded Python prototype files (`capture.py`, `hotkey_listener.py`, `overlay.py`, `popup.py`, `main.py`, `requirements.txt`) once Electron reaches equivalent functionality
- [ ] `win`/`createWindow()` in `main.js` (the Step 1 placeholder window) is now fully dead — its own comment predicted it might "become the basis for the Chat Panel at Step 5," but Step 5's Chat Panel was built as its own window instead (see decisions.md). Noted per CLAUDE.md's rule (mention dead code, don't delete unasked) rather than removed

## Next — Phase 2 (2.0–2.7), per implementation_plan.md

- [x] **2.0: Codebase reorg** (`main.js` slimmed; `src/overlay/`, `src/chatPanel/`, `src/shared/` introduced; dead Step 1 placeholder deleted) → verified 2026-08-24: `git mv` diff independently checked (paths, `require()`/`loadFile()` fixes, dead-code removal, `overlay.html`'s same-directory `overlay.js` reference); post-reorg smoke test passed both halves — `npm start` launches with no require/loadFile errors, hotkey registers, **and** a real hold-drag-release-type-close gesture completed end to end (log sequence, permanent crop/full files confirmed on disk with real matching content, temp dir empty after, new `conversations.jsonl` line schema-correct). Commit `8d787cf` on `phase-2`.
- [x] **2.1: Feedback capture — implemented and hands-on verified 2026-08-24/25.** 👍/👎 buttons in the Chat Panel title bar (`src/chatPanel/chatPanel.html`), persistent, settable once per conversation; **schema change added mid-step, per decisions.md's Decision 2** — an optional free-text `feedback_note` field revealed only on 👎, wired through `chatPanelPreload.js` → `main.js` (per-session closure, same pattern as `turns`) → `localLogger.js`. All 3 required scenarios confirmed via 4 real sessions, checked both in `conversations.jsonl` and on disk (permanent crop files present at every logged path): 👍/no-note (`feedback: "thumbs_up"`, `feedback_note: null`, note field never shown — 2 sessions), 👎/with-note (`feedback: "thumbs_down"`, `feedback_note: "that's interesting"`, matching exactly what was typed, no explicit submit step needed — it streams live), 👎/no-note (`feedback: "thumbs_down"`, `feedback_note: null`).
- [ ] 2.2: Main App Window shell — one persistent `BrowserWindow` (`mainWindow.html`), sidebar client-side view switching between Captures/Settings/Help, opened via dock icon/menu not the hotkey → verify: opening from the dock/menu shows the window; switching sidebar items swaps the view with no new window created; the hotkey/Overlay/Chat Panel flow is unaffected by whether this window is open. **Implementation note (resolved 2026-08-24, decisions.md)**: closing the window's own close button must not quit the app — already correct today via `window-all-closed`'s darwin no-op, no change needed there — but needs a new `app.on('activate', ...)` handler to recreate/show the window when the dock icon is clicked with no windows open (standard macOS lifecycle hook, doesn't exist yet)
- [ ] 2.3: Settings page → verify each control real, not just rendered:
      - Hotkey: changing it requires a real hold-drag-release gesture test before saving (reuses decisions.md's "registration success proves nothing" lesson) — failure/timeout behavior during that test still open, don't invent it
      - Display name: local-only field, explicitly not authentication
      - Theme: `nativeTheme` + `prefers-color-scheme`, light/dark/system, no new dependency
      - Dictation language: multilingual model swap off `base.en` — **folds in the earlier open question** (which whisper.cpp model size to bundle, base.en vs small.en) since they're now the same decision; `base.en` was only ever a provisional pick (2026-08-23) to get a working baseline, not a final call — needs its own accuracy verification pass per language actually offered, not assumed to work from architecture alone
      - App language: shown disabled ("coming soon"), not built
      - Data and privacy: storage location + open-folder action + clear-all action
- [ ] 2.4: Captures page — reverse-chronological list from `conversations.jsonl` (thumbnail, first question, timestamp, feedback icon if set) → verify: a real logged conversation appears in the list with correct thumbnail/text/timestamp
- [ ] 2.5: Help page — static in-app usage guide (hotkey, basic flow), no external link → verify: guide renders, no broken/placeholder links
- [ ] 2.6: Chat Panel enhancements → verify:
      - Captured-region thumbnail (data URL passed once at panel creation from the already-in-memory capture) — click opens it via `shell.openPath` in the system viewer
      - Hold-to-talk mic level indicator — real-time RMS computed inline in the existing `ScriptProcessorNode` callback, not a decorative loop animation; confirm it actually tracks real mic input level, not a canned animation
- [ ] 2.7: Active app detection (optional) — auto-tag `app_hint` via Quartz (macOS) → verify: a logged conversation's `app_hint` matches the actually-focused app at capture time
- [ ] Decide the Phase 3 format-conversion approach for multi-turn sessions (flatten to single-turn training rows vs. train for full multi-turn — see data_pipeline.md §8)

## Later / deferred — not blocking current work

- [ ] Marketing/dashboard website introducing the product, cluely.com-inspired — come back to this once Phase 1 has something worth showing off
- [ ] Screen Recording permission pre-prompt explanation screen (system_design_plan.md §7: "show an explanation screen *before* the OS prompt"). `capture.js` currently only checks `systemPreferences.getMediaAccessStatus('screen')` and fails loud if not granted — no onboarding UI. Deliberately deferred, not dropped; separate from Step 4's crop-correctness work.
- [ ] Startup crash-safety sweep for orphaned temp captures. Explicitly deferred during Step 5 (2026-08-22, user-approved) — the close-handler invariant (move-then-log or delete-and-discard) is what actually prevents dataset corruption; this sweep is a net for the narrower case of a crash/force-quit mid-session leaving files behind, not required for Step 5's own correctness. The subfolder it needs already exists and is in use: `capture.js` writes into `app.getPath('temp')/screen-region-captures/`, specifically so a launch-time sweep can safely delete-by-age (a day or two threshold is reasonable, per decisions.md) without ever touching an unrelated file some other app left in the shared OS temp root.
- [ ] Size-based storage cap for permanently-stored captures, only if storage volume ever actually becomes a problem (rough math says it won't for a long time — see decisions.md's temp-file-lifecycle row). If it ever does: prune oldest *unlabeled* (no thumbs-up) images first, never touch labeled ones. Not a calendar-based timer — that was explicitly rejected, see the same decisions.md row.
- [ ] **BLOCKING PRE-PACKAGING CHECKLIST ITEM for Phase 7 — do not ship a `.dmg` to a real user without doing this first.** Phase 7 packaging must bundle a **precompiled** `whisper-cli` binary (+ its `.dylib`s) with the app. Do **not** ship relying on compiling whisper.cpp from source on the end user's machine — `nodejs-whisper` does this by default on first use (needs Xcode CLI tools + `cmake`), which works invisibly well on a dev machine because the toolchain is already installed, but a packaged-app end user will not have one and the compile will fail outright. Flagged 2026-08-23 while evaluating the backend switch from `whisper-node-addon` (whose own failure mode — a broken prebuilt binary — is the mirror image of this one: both are packaging-fragility risks that are invisible until a real, cold-environment test catches them; see decisions.md for the full evaluation). **Verify this explicitly before any Phase 7 build goes to a real user** — build once during the release process, bundle the compiled binary with the app, and confirm a transcription actually works on a machine *without* Xcode CLI tools/`cmake` installed (a clean VM or a second machine, not just re-testing on this dev machine) before considering it done. Do not let this be discovered via a support report the way the rpath issue would have been if it had shipped.

## Open — Step 3 follow-ups (in priority order)

- [x] ~~Pick a 2-key, non-Option replacement accelerator.~~ **Done 2026-08-21 — `Control+1`**, after testing seven candidates hands-on. 11/11 activations, zero failures. See decisions.md for why each alternative was rejected and why registration success proves nothing.
- [ ] **Investigate "sometimes the hotkey needs a harder press"** — *probably already explained*: macOS requires the modifier down before the trigger key, so simultaneous presses often don't match. Confirm this fully accounts for it during normal use before closing.
- [x] ~~Investigate the auto-drawn zigzag on activation.~~ **Done 2026-08-21.** Root cause was not the suspected residual point (that hypothesis was measured and disproven) but mixing two coordinate sources — the main process's activation point vs the renderer's `clientX/clientY`, which disagree by a few px for the same stationary cursor. Fixed by anchoring the trail on the first mousemove. A follow-on 2px dot from zero-delta mousemoves was fixed with an exact-equality guard. Both verified against the three symptoms (still hold → no marks, real drag → correct bbox, badge persists until real movement). See decisions.md's consolidated hold/release section.
- [x] ~~Fix focus instability on activation.~~ **Closed 2026-08-21 — was a testing artifact, not a real defect.** The measurements behind it were taken with multiple Electron instances running at once (each `npm start` left the previous alive; every instance registered the same accelerator and fought over focus-steal). Measured cleanly on one instance: `isFocused` was `true` immediately on 32 of 33 gestures, vs `false` on 6 of 8 under contamination. Confirmed directly, not inferred: one physical keypress was logged by five separate instances within 2ms. See decisions.md §4 — kept as a section because the *methodology* lesson matters more than the non-finding.
- [ ] **Test whether `unregister()`-on-activate is still load-bearing.** Its supporting evidence was collected on the broken `Alt+Space` accelerator and is now explained by Option interception instead (see decisions.md). Remove it, run the gesture protocol, see if keyups still arrive. Stayed scoped to gesture+capture duration only, not stretched to session-length, per Step 5's concurrent-session-guard decision (2026-08-22, decisions.md) — that decision was made partly *because* extending this untested assumption's exposure window seemed like the wrong direction while it's still unverified.
- [ ] **Test whether `UNREGISTER_SETTLE_MS` can now be removed.** The original task that started all this. It was never justified — its stated rationale (fixing quick taps) is disproven — but removing it stayed unverifiable while nothing worked. There's finally a passing baseline to test against. Do this *after* the accelerator is settled, one variable at a time.
- [ ] **Decide accelerator configurability.** Three hardcoded accelerators in three iterations, each replaced for a reason the prior round's checks couldn't catch. Evidence now favours making it user-configurable over hunting for a better constant — see decisions.md.

**Closed 2026-08-21**: the `uiohook-napi` question. Its revisit trigger ("deliberate holds start losing keyups") appeared to fire, but the cause was the accelerator, not `globalShortcut`. Deliberate holds work. Stays deferred on its original terms.

## Open — robustness, not blocking Step 3

- [ ] **Investigate silent process death.** During Step 3 debugging (2026-08-20), the Electron app exited entirely mid-session — confirmed via `ps aux` showing no process, with nothing in the log explaining why (no crash trace, no uncaught-exception output; the log just stops). Happened around a stuck/unresolved gesture, but the causal link is unconfirmed — the timing may be coincidental. Not resolved by the stuck-gesture safety net, which addresses a stranded hotkey, not a dead process. Worth its own investigation once the hotkey/keyup thread is closed.

## Open questions blocking later phases

- [ ] Whether `image_context` (full screenshot) is always sent alongside the crop, or only conditionally — now relevant per-turn, not just once per session
- [ ] ~~Which whisper.cpp model size to bundle~~ — **folded into "Next — Phase 2" item 2.3's Dictation language sub-item (2026-08-24)**, since Settings' dictation-language control reopens exactly this tradeoff. How the model file gets distributed with the packaged app is still separately tracked under the blocking pre-packaging checklist item below.
- [x] ~~Blocking voice input: `whisper-node-addon` broken.~~ **Resolved 2026-08-23 — switched to `nodejs-whisper`.** Evaluated per the user's ask (re-verifying on-device + Metal, not assumed): both confirmed empirically via a real build + real transcription (`diagnostics/verify_nodejswhisper.js`) — Metal genuinely engaged at runtime (`ggml_metal_device_init: GPU name: MTL0 (Apple M1 Pro)`), zero network beyond the one-time model download, accurate transcript, ~775ms inference. `whisper-node-addon` uninstalled (also cleared the `npm audit` critical finding to zero). Full evidence in decisions.md. **Next: wire it into the app** — see the new item below.
      - [x] **`nodejs-whisper` wired into the two `TODO(next slice)` points in `chatPanel.html` — implemented and hands-on verified 2026-08-23.** Renderer captures raw PCM via a `ScriptProcessorNode` (requests `AudioContext({sampleRate:16000})` to avoid resampling) while `holding` is true, hands the raw Float32 buffer to main on the real keyup; main builds a 16kHz mono WAV by hand (`floatTo16BitWav()` — deliberately not relying on `nodejs-whisper`'s own ffmpeg-based conversion, since `ffmpeg` isn't installed and adding it would be a second Phase 7 packaging dependency, see decisions.md), applies the duration guard (`MIN_VOICE_DURATION_MS = 200`, keyed on captured audio length not hold time, per the user's refinement), calls `nodewhisper()`, sends the transcript back to populate `#input`. Mic-unavailable, too-short, and transcription-error each get a distinct, visible cue (`flashRecordingStatus()`) rather than a silent nothing. `extractTranscriptText()`'s shape confirmed empirically before relying on it (a plain string, not the `string[][]` its own `.d.ts` claims). **Hands-on verified across two sessions, 5 real dictations**: `sampleRate: 16000` honored exactly on real mic hardware (no resampling needed); all 5 transcribed accurately, ended by real keyup. **Two real gaps surfaced by testing, both fixed and re-verified**: (1) multiple dictations in one session were replacing the field instead of accumulating — fixed by appending; (2) the field was a single-line `<input>` that only showed the tail of a growing message once appending was fixed — switched to an auto-growing `<textarea>` with `Shift+Enter` for a literal newline. Re-verified after both fixes with 2 more real dictations, confirmed correct by the user. This closes Step 5's core loop.